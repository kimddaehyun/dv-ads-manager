# F-Brief (광고주 보고 문구 생성) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** F-MultiAccount 행 메뉴에서 계정 1개를 골라 기간을 지정하면, 카카오톡에 그대로 붙여넣을 수 있는 광고주 보고 문구 + 색칠된 분석 표 이미지를 블록 단위로 생성한다.

**Architecture:** 규칙 엔진(순수 함수)이 리포트 데이터에서 "말할 거리" 후보를 뽑고, AE가 체크 + 액션을 지정하면, Supabase Edge Function의 AI가 디브이 말투로 문장만 조립한다. AI는 분석가가 아니라 번역기다. 표는 캡처가 아니라 canvas로 생성한다. 데이터 수집은 F-Report의 `collectReportData()`를 공유하고 엑셀 생성만 건너뛴다.

**Tech Stack:** TypeScript 5.7 / Vite 6 / `@crxjs/vite-plugin` / native DOM (React 미사용) / canvas 2D / fetch / Supabase Edge Function / vitest (신규, 순수 로직 전용)

**설계 문서:** `docs/superpowers/specs/2026-07-16-f-brief-design.md` - 판단 근거는 전부 여기 있다. 읽고 시작할 것.

## Global Constraints

- **소스 수정 후 항상 `npm run build`** 로 `dist/` 갱신. 사용자가 `dist/`를 chrome://extensions에 로드해 쓰므로 빌드를 빼먹으면 변경이 반영되지 않는다. 문서만 고친 경우는 예외.
- **모든 사용자 노출 텍스트는 한글.** 영문 기술용어 금지 (`reload`/`fetch`/`background`/`백그라운드` 등). "페이지를 새로고침해 주세요" 같은 일상 한글로. `src/lib/friendly-error.ts` 패턴을 따른다.
- **em dash(`—`) / minus sign(`−` U+2212) 금지.** 모든 짝대기는 일반 하이픈 `-` (U+002D). 음수 표시(`(-230)`)도 동일.
- **콘텐츠 오버레이는 `dvads-` prefix.** `ads.naver.com` 호스트 CSS와 충돌 방지.
- **콘텐츠 스크립트는 순수 CSS** (`src/styles/overlay.css`). Tailwind 사용 금지. 옵션/팝업만 Tailwind.
- **오버레이 dropdown은 `createDropdown`(`src/content/ui-dropdown.ts`) 의무.** native `<select>` 금지.
- **backdrop dismiss는 `wireBackdropDismiss`(`src/content/dialog-dismiss.ts`) 의무.** 직접 구현 금지.
- **DV 주황 `#E6783B` 사용 면적 ~3% 이내.** 새 시각 패턴이 필요하면 `docs/DESIGN.md`를 먼저 갱신.
- **async 렌더는 await 완료 후 `DocumentFragment` 빌드 → `replaceChildren` atomic swap.** `innerHTML=""` 후 await하면 빈 화면이 노출된다.
- **테스트는 순수 로직에만.** DOM·chrome API·네이버 API는 수동 검증. 각 Task의 수동 검증 체크리스트를 실제로 수행할 것.
- 커밋 메시지 말미: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## File Structure

**신규 (순수 로직 - 테스트 대상)**
- `src/lib/brief-rules.ts` - ROAS 구간 정의 + 후보 추출. chrome/DOM/네트워크 무의존.
- `src/lib/brief-rules.test.ts` - 위의 테스트.
- `src/lib/brief-verify.ts` - AI 응답 숫자 검산. 순수.
- `src/lib/brief-verify.test.ts` - 위의 테스트.

**신규 (부수효과 있음 - 수동 검증)**
- `src/lib/brief-table.ts` - `BriefTableSpec` → canvas → PNG Blob.
- `src/lib/brief-data.ts` - `collectReportData` + F-Brief 전용 추가 수집(전기 상품).
- `src/lib/brief-compose.ts` - Edge Function 호출 + 검산 적용.
- `src/content/brief.ts` - 진입 오케스트레이터 (report.ts 패턴 미러).
- `src/content/brief-panel.ts` - 패널 DOM 렌더.

**수정**
- `src/lib/report-build.ts` - `collectReportData()` 추출 (Task 4).
- `src/types/storage.ts` - `MultiAccountUserMeta.targetRoas` 추가 (Task 5).
- `src/options/multi-account-ui.tsx` - 목표 ROAS 입력 (Task 5).
- `src/content/multi-account.ts` - 행 메뉴 "보고 문구" 추가 (Task 6).
- `src/content/report-datepicker.ts` - `showAuthor?: boolean` 추가 (Task 6).
- `src/styles/overlay.css` - 패널 스타일 (Task 6, 10).
- `manifest.config.ts` - Supabase host_permissions (Task 9).

**신규 (서버)**
- `supabase/functions/brief-compose/index.ts` - Edge Function.

---

## Task 순서 근거

**Task 1~3은 Supabase·AI와 무관하다.** 서버 인프라를 기다리지 않는다. 설계 §4 모듈 경계 원칙 - 가치의 8할이 규칙 엔진과 표 생성에 있다.

**Task 6이 끝나면 AI 없이도 쓸 수 있는 기능이 된다** (후보 목록 + 색칠 표 복사). Task 7~10은 그 위에 얹는다.

---

## Task 1: vitest 도입 + ROAS 구간 정의

**Files:**
- Create: `vitest.config.ts`
- Create: `src/lib/brief-rules.ts`
- Test: `src/lib/brief-rules.test.ts`
- Modify: `package.json` (scripts + devDependencies)

**Interfaces:**
- Consumes: `ReportMetrics` from `@/lib/report-data`
- Produces:
  - `type RoasBand = "green" | "yellow" | "none"`
  - `const YELLOW_FLOOR_RATIO = 0.75`
  - `function roasBand(roas: number, target: number): RoasBand`
  - `function roasPct(m: ReportMetrics): number`

**왜 별도 `vitest.config.ts`인가:** 기존 `vite.config.ts`는 `crx({ manifest })` 플러그인을 쓴다. 이건 MV3 확장을 빌드하는 플러그인이라 테스트 실행 시 manifest를 읽고 확장 번들링을 시도해 깨진다. 테스트용 설정을 분리하고 alias만 가져온다.

**globals 미사용:** `describe`/`it`/`expect`를 `vitest`에서 명시적으로 import한다. `globals: true`를 쓰면 `tsconfig.app.json`에 `"types": ["vitest/globals"]`를 추가해야 하는데, 기존 tsconfig를 안 건드리는 쪽이 안전하다.

- [ ] **Step 1: vitest 설치**

```bash
npm i -D vitest@^3.0.0
```

- [ ] **Step 2: `vitest.config.ts` 생성**

```ts
// 테스트 전용 Vite 설정 — 기존 vite.config.ts는 crx() 플러그인이 MV3 확장 번들링을
// 시도해 테스트에서 깨진다. 경로 별칭(@/)만 동일하게 맞추고 나머지는 최소로.
// 대상은 순수 로직(brief-rules/brief-verify)뿐 — DOM·chrome API·네이버 API는 수동 검증.
import { defineConfig } from "vitest/config";
import path from "node:path";

export default defineConfig({
  resolve: {
    alias: { "@": path.resolve(__dirname, "src") },
  },
  test: {
    include: ["src/**/*.test.ts"],
    environment: "node",
  },
});
```

- [ ] **Step 3: `package.json`에 스크립트 추가**

`"scripts"` 안에 `"typecheck"` 다음 줄로 추가:

```json
    "test": "vitest run",
    "test:watch": "vitest",
```

- [ ] **Step 4: 실패하는 테스트 작성**

`src/lib/brief-rules.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { roasBand, roasPct, YELLOW_FLOOR_RATIO } from "./brief-rules";
import { ZERO_METRICS } from "./report-data";

describe("roasPct", () => {
  it("매출/비용 x 100", () => {
    expect(roasPct({ ...ZERO_METRICS, cost: 100_000, revenue: 620_000 })).toBeCloseTo(620);
  });

  it("비용 0이면 0 — 0으로 나누기 방지", () => {
    expect(roasPct({ ...ZERO_METRICS, cost: 0, revenue: 50_000 })).toBe(0);
  });
});

describe("roasBand", () => {
  // 목표 800% 기준. 노랑 하한 = 800 x 0.75 = 600.
  // 근거: 임유엽 AE 보고 로그 — 초록 "800% 이상 상향" / 노랑 "600%대 유지" / 무색 "목표 미달 하향".
  it("목표 이상이면 green", () => {
    expect(roasBand(900, 800)).toBe("green");
  });

  it("목표와 정확히 같으면 green — 경계 포함", () => {
    expect(roasBand(800, 800)).toBe("green");
  });

  it("목표 미만 노랑 하한 이상이면 yellow", () => {
    expect(roasBand(700, 800)).toBe("yellow");
  });

  it("노랑 하한과 정확히 같으면 yellow — 경계 포함", () => {
    // 이 경계가 어긋나면 유지해야 할 키워드가 "하향" 후보로 광고주에게 나간다.
    expect(roasBand(600, 800)).toBe("yellow");
  });

  it("노랑 하한 바로 아래면 none", () => {
    expect(roasBand(599, 800)).toBe("none");
  });

  it("0이면 none", () => {
    expect(roasBand(0, 800)).toBe("none");
  });

  it("목표가 0 이하면 판정 불가 — none", () => {
    expect(roasBand(500, 0)).toBe("none");
  });

  it("YELLOW_FLOOR_RATIO는 0.75", () => {
    expect(YELLOW_FLOOR_RATIO).toBe(0.75);
  });
});
```

- [ ] **Step 5: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL - `Failed to resolve import "./brief-rules"`

- [ ] **Step 6: 최소 구현**

`src/lib/brief-rules.ts`:

```ts
/**
 * F-Brief 규칙 엔진 — 리포트 데이터에서 "말할 거리" 후보를 뽑는다. AI 미사용.
 *
 * 이 파일은 순수 함수만 담는다 — chrome API·DOM·네트워크 무의존. 그래서 테스트할 수 있고,
 * Supabase/AI 인프라 없이 먼저 만들어 쓸 수 있다(설계 §4 모듈 경계).
 *
 * 설계: docs/superpowers/specs/2026-07-16-f-brief-design.md §5
 */

import { type ReportMetrics } from "./report-data";

/** 노랑(유지) 구간의 하한 = 목표 x 이 비율. 임유엽 AE 로그(목표 800% / 노랑 600%대)에서 역산. */
export const YELLOW_FLOOR_RATIO = 0.75;

/**
 * ROAS 구간. 색칠과 후보 조건이 **이 함수 하나**를 공유한다 — 두 곳에 따로 쓰면 어긋난다.
 *
 * green  = 목표 달성. 순위 여유가 있으면 상향 여지.
 * yellow = 목표엔 못 미치나 유지할 만함. **하향 대상이 아니다.**
 * none   = 하향 검토.
 */
export type RoasBand = "green" | "yellow" | "none";

export function roasBand(roas: number, target: number): RoasBand {
  // 목표 미설정(0/음수)이면 판정 자체가 불가능 — 호출부가 후보를 만들지 않아야 한다.
  if (!Number.isFinite(target) || target <= 0) return "none";
  if (roas >= target) return "green";
  if (roas >= target * YELLOW_FLOOR_RATIO) return "yellow";
  return "none";
}

/** 광고수익률(%) = 매출액 / 총비용 x 100. 비용 0이면 0(0으로 나누기 방지). */
export function roasPct(m: ReportMetrics): number {
  if (m.cost <= 0) return 0;
  return (m.revenue / m.cost) * 100;
}
```

- [ ] **Step 7: 테스트 통과 확인**

Run: `npm test`
Expected: PASS - 9 passed

- [ ] **Step 8: 타입 체크**

Run: `npm run typecheck`
Expected: 에러 없음

- [ ] **Step 9: 커밋**

```bash
git add vitest.config.ts package.json package-lock.json src/lib/brief-rules.ts src/lib/brief-rules.test.ts
git commit -m "$(cat <<'EOF'
feat(F-Brief): vitest 도입 + ROAS 구간 정의(roasBand)

색칠과 후보 조건이 roasBand() 하나를 공유 — 두 곳에 따로 쓰면 어긋난다.
노랑 하한 = 목표 x 0.75 (임유엽 AE 로그 800%/600%대에서 역산).

테스트는 순수 로직 전용. crx() 플러그인이 테스트에서 깨지므로 vitest.config.ts 분리.
경계값(roas == 목표, roas == 목표 x 0.75)은 실제 계정으로 재현이 거의 불가능해
자동 확인이 유일한 검증 수단.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: 후보 추출 규칙

**Files:**
- Modify: `src/lib/brief-rules.ts`
- Test: `src/lib/brief-rules.test.ts`

**Interfaces:**
- Consumes: `roasBand` / `roasPct` (Task 1), `ReportMetrics` from `@/lib/report-data`, `KeywordGroup` from `@/lib/report-variable`, `NamedMetrics` from `@/lib/report-fill`
- Produces:
  - `type BriefKind`, `type BriefAction`, `interface BriefTableRow`, `interface BriefTableSpec`, `interface BriefCandidate`, `interface BriefRuleOpts`
  - `const COST_FLOOR = 10_000`
  - `function flattenKeywords(groups: KeywordGroup[]): BriefKeywordRow[]`
  - `function extractCandidates(input: BriefRuleInput): BriefCandidate[]`

**설계 §5의 후보 5종 중 4종을 여기서 만든다.** `highRoasLowRank`는 순위 데이터가 필요해 Task 7, `productConvDrop`은 전기 상품 수집이 필요해 Task 8.

**`belowTargetKeyword`가 `roas < 목표`가 아닌 이유:** 그러면 노랑(유지 대상)까지 "목표 미달"로 딸려 들어간다. AE는 노랑을 하향하지 않는다. 액션이 다르면 같은 후보가 아니다. 설계 §5 참조.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/brief-rules.test.ts` 맨 아래에 추가:

```ts
import { flattenKeywords, extractCandidates, COST_FLOOR } from "./brief-rules";
import { type KeywordGroup } from "./report-variable";

// 테스트 픽스처 헬퍼 — cost/conv/revenue만 지정하면 나머지는 0.
function m(cost: number, purchaseConv: number, revenue: number): ReportMetrics {
  return { ...ZERO_METRICS, cost, purchaseConv, revenue };
}

const GROUPS: KeywordGroup[] = [
  {
    campaign: "[DV] 대나무",
    group: "1. 기본 상품명",
    keywords: [
      // 비용 1.2만, 전환 0 → zeroConvKeyword
      { keyword: "대나무돗자리", metrics: m(12_000, 0, 0) },
      // 비용 5만, ROAS 900% (목표 800 초과) → green, 후보 아님(순위는 Task 7)
      { keyword: "대나무자리", metrics: m(50_000, 5, 450_000) },
      // 비용 5만, ROAS 700% → yellow → belowTarget 후보 아님 (유지 대상)
      { keyword: "여름돗자리", metrics: m(50_000, 4, 350_000) },
      // 비용 5만, ROAS 400% → none → belowTargetKeyword
      { keyword: "돗자리추천", metrics: m(50_000, 2, 200_000) },
      // 비용 5천(임계 미만), 전환 0 → 후보 아님
      { keyword: "소액키워드", metrics: m(5_000, 0, 0) },
    ],
  },
];

describe("flattenKeywords", () => {
  it("그룹 계층을 평탄화하고 캠페인/그룹을 각 행에 붙인다", () => {
    const rows = flattenKeywords(GROUPS);
    expect(rows).toHaveLength(5);
    expect(rows[0]).toMatchObject({
      keyword: "대나무돗자리",
      campaign: "[DV] 대나무",
      group: "1. 기본 상품명",
    });
  });
});

describe("extractCandidates", () => {
  const base = { keywords: GROUPS, placements: [] as NamedMetrics[], targetRoas: 800 };

  it("COST_FLOOR는 1만원", () => {
    expect(COST_FLOOR).toBe(10_000);
  });

  it("비용 임계 이상 + 전환 0 → zeroConvKeyword", () => {
    const c = extractCandidates(base).find((x) => x.kind === "zeroConvKeyword");
    expect(c).toBeDefined();
    expect(c!.facts.keywords).toBe("대나무돗자리");
    expect(c!.facts.count).toBe(1);
  });

  it("비용 임계 미만은 전환 0이어도 후보에서 제외", () => {
    const c = extractCandidates(base).find((x) => x.kind === "zeroConvKeyword");
    expect(String(c!.facts.keywords)).not.toContain("소액키워드");
  });

  it("노랑 구간 키워드는 belowTargetKeyword에 안 들어간다", () => {
    // 이게 깨지면 유지해야 할 키워드에 "하향하겠습니다"가 광고주에게 나간다.
    const c = extractCandidates(base).find((x) => x.kind === "belowTargetKeyword");
    expect(c).toBeDefined();
    expect(String(c!.facts.keywords)).not.toContain("여름돗자리");
    expect(String(c!.facts.keywords)).toContain("돗자리추천");
  });

  it("전환 0 키워드는 belowTargetKeyword와 중복되지 않는다", () => {
    // 전환 0이면 ROAS도 0이라 none 구간에 자동으로 떨어진다. 그건 이미 별도 후보다.
    const c = extractCandidates(base).find((x) => x.kind === "belowTargetKeyword");
    expect(String(c!.facts.keywords)).not.toContain("대나무돗자리");
  });

  it("목표 ROAS 미설정이면 분류 후보를 만들지 않는다", () => {
    const cands = extractCandidates({ ...base, targetRoas: undefined });
    expect(cands.find((x) => x.kind === "belowTargetKeyword")).toBeUndefined();
    // 목표와 무관한 후보는 그대로 나온다.
    expect(cands.find((x) => x.kind === "zeroConvKeyword")).toBeDefined();
  });

  it("지면 비용 임계 이상 + 전환 0 → zeroConvPlacement", () => {
    const placements: NamedMetrics[] = [
      { label: "네이버 메인", metrics: m(31_000, 0, 0) },
      { label: "네이버 검색", metrics: m(80_000, 10, 800_000) },
      { label: "기타", metrics: m(500, 0, 0) },
    ];
    const c = extractCandidates({ ...base, placements }).find((x) => x.kind === "zeroConvPlacement");
    expect(c!.facts.placements).toBe("네이버 메인");
  });

  it("후보의 표는 kind가 결정한다 — 행에 band가 칠해진다", () => {
    const c = extractCandidates(base).find((x) => x.kind === "belowTargetKeyword");
    expect(c!.table.rows.some((r) => r.band === "none")).toBe(true);
  });

  it("기본 선택은 전부 해제 — AE선택 모드의 시작 상태", () => {
    expect(extractCandidates(base).every((c) => c.selected === false)).toBe(true);
  });

  it("후보가 없으면 빈 배열", () => {
    expect(extractCandidates({ keywords: [], placements: [], targetRoas: 800 })).toEqual([]);
  });
});
```

- [ ] **Step 2: 테스트가 실패하는지 확인**

Run: `npm test`
Expected: FAIL - `flattenKeywords is not exported` 등

- [ ] **Step 3: 구현**

`src/lib/brief-rules.ts`에 추가 (기존 import 줄을 아래로 교체):

```ts
import { type ReportMetrics } from "./report-data";
import { type KeywordGroup } from "./report-variable";
import { type NamedMetrics } from "./report-fill";
```

파일 맨 아래에 추가:

```ts
// ── 후보 추출 ──

/** 비용 임계 — 보고 로그 5건 중 4건이 "1만원 이상" 문구를 쓴다. */
export const COST_FLOOR = 10_000;

export type BriefKind =
  | "zeroConvKeyword"      // 비용 임계 이상인데 전환 0
  | "highRoasLowRank"      // 목표 달성인데 순위가 낮음 (Task 7)
  | "belowTargetKeyword"   // 전환은 있으나 none 구간
  | "zeroConvPlacement"    // 지면 비용 임계 이상인데 전환 0
  | "productConvDrop";     // 전기 대비 전환 빠진 상품 (Task 8)

/** AE가 고르는 액션. AI가 창작하지 않는다 — 완전자동 모드에서도 이 목록에서만 고른다. */
export type BriefAction = "raise" | "hold" | "lower" | "exclude" | "ask" | "custom";

export interface BriefTableRow {
  cells: string[];
  /** 행 배경색. 없으면 무색. */
  band?: RoasBand;
}

/** 표 명세 — brief-table.ts가 이것만 보고 그린다. 규칙 로직을 알 필요가 없다. */
export interface BriefTableSpec {
  title: string;
  columns: string[];
  rows: BriefTableRow[];
}

export interface BriefCandidate {
  kind: BriefKind;
  /** 문구에 들어갈 사실. **AI에게는 이것만 전달된다** (설계 §3 2겹). */
  facts: Record<string, string | number>;
  /** 딸려나올 표. kind가 결정한다 — AE가 고르지 않는다. */
  table: BriefTableSpec;
  selected: boolean;
  action?: BriefAction;
  /** action === "custom"일 때만. */
  actionText?: string;
}

export interface BriefKeywordRow {
  keyword: string;
  campaign: string;
  group: string;
  metrics: ReportMetrics;
  /** 추정 순위. Task 7에서 채운다. 없으면 순위 후보를 만들지 않는다. */
  rank?: number;
}

export interface BriefRuleInput {
  keywords: KeywordGroup[];
  placements: NamedMetrics[];
  /** 계정별 목표 광고수익률(%). undefined면 구간 분류 후보를 만들지 않는다. */
  targetRoas?: number;
}

/** 그룹 계층(캠페인 > 그룹 > 키워드)을 행 목록으로 평탄화. 캠페인/그룹을 각 행에 붙인다. */
export function flattenKeywords(groups: KeywordGroup[]): BriefKeywordRow[] {
  const out: BriefKeywordRow[] = [];
  for (const g of groups) {
    for (const k of g.keywords) {
      out.push({ keyword: k.keyword, campaign: g.campaign, group: g.group, metrics: k.metrics });
    }
  }
  return out;
}

const KW_COLUMNS = ["키워드", "노출", "클릭", "총비용", "구매완료", "매출액", "수익률"];

function kwRow(r: BriefKeywordRow, target?: number): BriefTableRow {
  const roas = roasPct(r.metrics);
  return {
    cells: [
      r.keyword,
      r.metrics.impressions.toLocaleString(),
      r.metrics.clicks.toLocaleString(),
      `${r.metrics.cost.toLocaleString()}원`,
      String(r.metrics.purchaseConv),
      `${r.metrics.revenue.toLocaleString()}원`,
      `${roas.toFixed(0)}%`,
    ],
    band: target != null ? roasBand(roas, target) : undefined,
  };
}

/** 비용 많이 쓴 순. 표는 상위가 먼저 보여야 한다. */
function byCostDesc<T extends { metrics: ReportMetrics }>(a: T, b: T): number {
  return b.metrics.cost - a.metrics.cost;
}

export function extractCandidates(input: BriefRuleInput): BriefCandidate[] {
  const { targetRoas } = input;
  const rows = flattenKeywords(input.keywords);
  const out: BriefCandidate[] = [];

  // ① 비용 임계 이상인데 전환 0인 키워드
  const zeroConv = rows.filter((r) => r.metrics.cost >= COST_FLOOR && r.metrics.purchaseConv === 0)
    .sort(byCostDesc);
  if (zeroConv.length > 0) {
    out.push({
      kind: "zeroConvKeyword",
      facts: {
        기준: `광고비 ${COST_FLOOR.toLocaleString()}원 이상 소진, 구매완료 전환 0건`,
        keywords: zeroConv.map((r) => r.keyword).join(", "),
        count: zeroConv.length,
        비용합계: zeroConv.reduce((s, r) => s + r.metrics.cost, 0),
      },
      table: {
        title: `광고비 ${COST_FLOOR.toLocaleString()}원 이상 · 전환 0 키워드`,
        columns: KW_COLUMNS,
        rows: zeroConv.map((r) => kwRow(r, targetRoas)),
      },
      selected: false,
    });
  }

  // ② 전환은 있으나 none 구간(하향 검토). 목표 미설정이면 판정 불가라 만들지 않는다.
  // 전환 0은 ①에서 이미 다뤘으므로 purchaseConv > 0으로 제외 — 중복 후보 방지.
  if (targetRoas != null && targetRoas > 0) {
    const below = rows.filter((r) =>
      r.metrics.cost >= COST_FLOOR &&
      r.metrics.purchaseConv > 0 &&
      roasBand(roasPct(r.metrics), targetRoas) === "none",
    ).sort(byCostDesc);
    if (below.length > 0) {
      out.push({
        kind: "belowTargetKeyword",
        facts: {
          기준: `목표 수익률 ${targetRoas}% 미달`,
          keywords: below.map((r) => r.keyword).join(", "),
          count: below.length,
          비용합계: below.reduce((s, r) => s + r.metrics.cost, 0),
        },
        table: {
          title: `목표 수익률 ${targetRoas}% 미달 키워드`,
          columns: KW_COLUMNS,
          rows: below.map((r) => kwRow(r, targetRoas)),
        },
        selected: false,
      });
    }
  }

  // ③ 지면 비용 임계 이상인데 전환 0
  const zeroPlace = input.placements
    .filter((p) => p.metrics.cost >= COST_FLOOR && p.metrics.purchaseConv === 0)
    .sort(byCostDesc);
  if (zeroPlace.length > 0) {
    out.push({
      kind: "zeroConvPlacement",
      facts: {
        기준: `광고비 ${COST_FLOOR.toLocaleString()}원 이상 소진, 구매완료 전환 0건`,
        placements: zeroPlace.map((p) => p.label).join(", "),
        count: zeroPlace.length,
        비용합계: zeroPlace.reduce((s, p) => s + p.metrics.cost, 0),
      },
      table: {
        title: "지면별 성과",
        columns: ["지면", "노출", "클릭", "총비용", "구매완료", "매출액", "수익률"],
        rows: input.placements.sort(byCostDesc).map((p) => ({
          cells: [
            p.label,
            p.metrics.impressions.toLocaleString(),
            p.metrics.clicks.toLocaleString(),
            `${p.metrics.cost.toLocaleString()}원`,
            String(p.metrics.purchaseConv),
            `${p.metrics.revenue.toLocaleString()}원`,
            `${roasPct(p.metrics).toFixed(0)}%`,
          ],
          band: targetRoas != null ? roasBand(roasPct(p.metrics), targetRoas) : undefined,
        })),
      },
      selected: false,
    });
  }

  return out;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS - 모든 테스트 통과

- [ ] **Step 5: 타입 체크**

Run: `npm run typecheck`
Expected: 에러 없음

- [ ] **Step 6: 커밋**

```bash
git add src/lib/brief-rules.ts src/lib/brief-rules.test.ts
git commit -m "$(cat <<'EOF'
feat(F-Brief): 후보 추출 규칙 4종 (키워드 전환0 / 목표미달 / 지면 전환0)

표 종류는 kind가 결정 — AE가 고르지 않는다.
belowTargetKeyword는 roas < 목표가 아니라 none 구간 — 노랑(유지 대상)이
딸려 들어가면 "하향하겠습니다"가 광고주에게 잘못 나간다.
전환 0은 zeroConvKeyword가 이미 다루므로 purchaseConv > 0으로 중복 제거.

목표 ROAS 미설정이면 구간 분류 후보를 만들지 않는다(자동 추정 안 함 - 계정이
통째로 부진하면 전부 "정상"으로 나와 무용지물).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: 분석 표 이미지 생성

**Files:**
- Create: `src/lib/brief-table.ts`

**Interfaces:**
- Consumes: `BriefTableSpec` / `BriefTableRow` / `RoasBand` (Task 2)
- Produces:
  - `const MAX_TABLE_ROWS = 20`
  - `async function renderTablePng(spec: BriefTableSpec): Promise<Blob>`
  - `async function copyTablePng(spec: BriefTableSpec): Promise<void>`

**캡처가 아니라 생성이다.** 표의 데이터를 우리가 이미 갖고 있다. 스크롤에 안 잘리고, 네이버 DOM 셀렉터가 바뀌어도 무관하고, 색칠이 자동이다. 설계 §9.

**테스트하지 않는다** - canvas는 DOM API라 node 환경에서 못 돈다. 수동 검증.

- [ ] **Step 1: 구현**

`src/lib/brief-table.ts`:

```ts
/**
 * F-Brief 분석 표 이미지 — BriefTableSpec을 canvas에 그려 PNG로.
 *
 * **캡처가 아니라 생성이다.** 표 데이터를 우리가 이미 갖고 있어 화면을 찍을 이유가 없다.
 * 스크롤에 안 잘리고, ads.naver.com DOM이 바뀌어도 무관하고, 색칠이 자동이다.
 * AE가 엑셀 열어서 손으로 칠하던 작업이 여기서 사라진다.
 *
 * 설계: docs/superpowers/specs/2026-07-16-f-brief-design.md §9
 */

import { type BriefTableSpec, type BriefTableRow, type RoasBand } from "./brief-rules";

/** 행 수 상한. 초과분은 "외 N개"로 접되 **잘린 사실을 표에 명시**한다(조용한 절단 금지). */
export const MAX_TABLE_ROWS = 20;

const PAD = 16;
const ROW_H = 30;
const HEAD_H = 34;
const TITLE_H = 34;
const FONT = "Pretendard, -apple-system, sans-serif";

// 행 배경 — DESIGN.md Semantic (State Colors)를 10% 투명도로 재사용.
// success #16a34a / warning #d97706. DV 주황은 쓰지 않는다(3% 규칙).
const BAND_BG: Record<RoasBand, string> = {
  green: "rgba(22, 163, 74, 0.10)",
  yellow: "rgba(217, 119, 6, 0.10)",
  none: "transparent",
};

/** 열 너비 — 각 열의 최장 텍스트를 실측해 정한다. 첫 열(라벨)은 넓게, 지표는 우측 정렬. */
function measureCols(ctx: CanvasRenderingContext2D, spec: BriefTableSpec, rows: BriefTableRow[]): number[] {
  const widths: number[] = [];
  for (let c = 0; c < spec.columns.length; c++) {
    ctx.font = `600 13px ${FONT}`;
    let max = ctx.measureText(spec.columns[c]).width;
    ctx.font = `400 13px ${FONT}`;
    for (const r of rows) {
      max = Math.max(max, ctx.measureText(r.cells[c] ?? "").width);
    }
    widths.push(Math.ceil(max) + 20);
  }
  return widths;
}

/** 상한 초과 시 비용 상위 MAX_TABLE_ROWS개 + "외 N개" 행. 잘린 사실을 표에 남긴다. */
function capRows(spec: BriefTableSpec): BriefTableRow[] {
  if (spec.rows.length <= MAX_TABLE_ROWS) return spec.rows;
  const kept = spec.rows.slice(0, MAX_TABLE_ROWS);
  const hidden = spec.rows.length - MAX_TABLE_ROWS;
  const filler = new Array(Math.max(0, spec.columns.length - 1)).fill("");
  kept.push({ cells: [`외 ${hidden}개`, ...filler] });
  return kept;
}

export async function renderTablePng(spec: BriefTableSpec): Promise<Blob> {
  // 폰트가 로드되기 전에 그리면 fallback 폰트로 그려져 폭 측정이 어긋난다.
  await document.fonts.ready;

  const rows = capRows(spec);
  const probe = document.createElement("canvas").getContext("2d");
  if (!probe) throw new Error("표 이미지를 만들지 못했어요");
  const cols = measureCols(probe, spec, rows);

  const w = PAD * 2 + cols.reduce((a, b) => a + b, 0);
  const h = PAD * 2 + TITLE_H + HEAD_H + rows.length * ROW_H;

  // 카톡에서 흐릿하지 않게 화면 배율만큼 크게 그린다.
  const dpr = Math.max(1, Math.min(3, window.devicePixelRatio || 1));
  const canvas = document.createElement("canvas");
  canvas.width = Math.ceil(w * dpr);
  canvas.height = Math.ceil(h * dpr);
  const ctx = canvas.getContext("2d");
  if (!ctx) throw new Error("표 이미지를 만들지 못했어요");
  ctx.scale(dpr, dpr);

  ctx.fillStyle = "#ffffff";
  ctx.fillRect(0, 0, w, h);

  // 제목
  ctx.fillStyle = "#171717";
  ctx.font = `600 15px ${FONT}`;
  ctx.textBaseline = "middle";
  ctx.fillText(spec.title, PAD, PAD + TITLE_H / 2);

  let y = PAD + TITLE_H;

  // 헤더
  ctx.fillStyle = "#f4f5f7";
  ctx.fillRect(PAD, y, w - PAD * 2, HEAD_H);
  ctx.fillStyle = "#171717";
  ctx.font = `600 13px ${FONT}`;
  let x = PAD;
  for (let c = 0; c < spec.columns.length; c++) {
    ctx.textAlign = c === 0 ? "left" : "right";
    ctx.fillText(spec.columns[c], c === 0 ? x + 10 : x + cols[c] - 10, y + HEAD_H / 2);
    x += cols[c];
  }
  y += HEAD_H;

  // 본문
  ctx.font = `400 13px ${FONT}`;
  for (const r of rows) {
    if (r.band && r.band !== "none") {
      ctx.fillStyle = BAND_BG[r.band];
      ctx.fillRect(PAD, y, w - PAD * 2, ROW_H);
    }
    ctx.fillStyle = "#171717";
    x = PAD;
    for (let c = 0; c < spec.columns.length; c++) {
      ctx.textAlign = c === 0 ? "left" : "right";
      ctx.fillText(r.cells[c] ?? "", c === 0 ? x + 10 : x + cols[c] - 10, y + ROW_H / 2);
      x += cols[c];
    }
    // 행 구분선
    ctx.strokeStyle = "#eef0f3";
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(PAD, y + ROW_H - 0.5);
    ctx.lineTo(w - PAD, y + ROW_H - 0.5);
    ctx.stroke();
    y += ROW_H;
  }

  return await new Promise<Blob>((resolve, reject) => {
    canvas.toBlob((b) => (b ? resolve(b) : reject(new Error("표 이미지를 만들지 못했어요"))), "image/png");
  });
}

/** 표를 PNG로 만들어 클립보드에. AE는 카카오톡에서 붙여넣기만 하면 된다. */
export async function copyTablePng(spec: BriefTableSpec): Promise<void> {
  const blob = await renderTablePng(spec);
  await navigator.clipboard.write([new ClipboardItem({ "image/png": blob })]);
}
```

- [ ] **Step 2: 타입 체크**

Run: `npm run typecheck`
Expected: 에러 없음

- [ ] **Step 3: 빌드**

Run: `npm run build`
Expected: 성공, `dist/` 갱신

- [ ] **Step 4: 커밋**

```bash
git add src/lib/brief-table.ts
git commit -m "$(cat <<'EOF'
feat(F-Brief): 분석 표 canvas 렌더 + 클립보드 PNG 복사

캡처가 아니라 생성 — 표 데이터를 이미 갖고 있어 화면을 찍을 이유가 없다.
스크롤 잘림·DOM 셀렉터 파손·수동 색칠이 전부 사라진다.

document.fonts.ready 후 렌더(안 하면 fallback 폰트로 폭 측정이 어긋남).
devicePixelRatio 반영(카톡에서 흐릿하면 안 됨).
20행 초과 시 "외 N개" 행으로 잘린 사실을 표에 명시 — 조용한 절단 금지.
색은 DESIGN.md Semantic 10% 재사용, DV 주황 미사용(3% 규칙).

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: `collectReportData()` 추출

**Files:**
- Modify: `src/lib/report-build.ts:640-800` (`buildReportBytes`)

**Interfaces:**
- Produces:
  - `interface ReportData`
  - `async function collectReportData(target: ReportTarget, range: DateRange, meta: ReportMeta): Promise<ReportData>`
- `buildReportBytes`의 시그니처와 동작은 **불변**.

**왜:** 현재 `buildReportBytes`는 데이터 수집과 엑셀 생성을 한 함수에서 섞어서 한다. F-Brief는 수집만 필요하고 엑셀은 필요 없다. 설계 §4.1.

**절대 건드리지 말 것:** 기존 병렬 구조(`brandP`/`displayDataP`를 await로 막지 않고 promise로 먼저 출발시키는 패턴, `report-build.ts:648-660` 주석). 성능 감사 2026-07-02에서 확정된 구조다. **수집 순서와 동시성을 그대로 옮기기만 한다.**

**추가로 노출하는 것:** `shProductAdRows`(소재ID 기준)와 `shProductInfo`(ID → 이름/URL). Task 8이 전기 상품과 소재ID로 매칭하려면 필요하다. `shProducts`(ProductRow)는 이름으로 조인된 뒤라 ID가 사라진다. 이미 함수 안에서 계산되는 값이라 노출은 공짜다.

- [ ] **Step 1: 리팩터 전 기준 파일 확보**

확장을 로드한 상태에서 `ads.naver.com` 계정 목록 → 광고주 1개 → "리포트 생성" → 기간 **지난달** → 다운로드.

파일을 `C:\Users\PC\AppData\Local\Temp\claude\C--dev-dvads\baseline.xlsx`로 옮긴다. 광고주명·기간을 메모해둔다 (Step 6에서 동일 조건으로 다시 뽑아야 한다).

- [ ] **Step 2: `ReportData` 타입 + `collectReportData` 추출**

`src/lib/report-build.ts`의 `buildReportBytes` **직전**에 추가:

```ts
/** F-Report(엑셀)와 F-Brief(문구)가 공유하는 수집 결과. 엑셀 생성과 무관하다. */
export interface ReportData {
  model: ReportModel;
  searchTypes: SummaryType[];
  displayData: GfaData;
  campGroups: CampaignTypeGroup[];
  plKeywords: KeywordGroup[];
  shKeywords: KeywordGroup[];
  shProducts: ProductRow[];
  /** 소재ID 기준 상품 지표(이름 조인 전). F-Brief가 전기와 ID로 매칭할 때 쓴다. */
  shProductAdRows: NamedMetrics[];
  /** 소재ID → 상품명/URL. */
  shProductInfo: Map<string, ProductInfo>;
}
```

그 다음 `buildReportBytes`의 본문에서 **양식 fetch/openXlsx(1단계)와 fill/render(6단계)를 제외한 전부**를 `collectReportData`로 옮긴다. 즉:

- `const brandP = fetchBrandContracts(cid)...` 부터
- `const shProducts = buildProductRows(...)` 까지

를 그대로 잘라내 새 함수 본문으로 만들고, 마지막에 `ReportData`를 반환한다. **코드 내용·순서·주석을 한 글자도 바꾸지 않는다.**

```ts
export async function collectReportData(
  target: ReportTarget, range: DateRange, meta: ReportMeta,
): Promise<ReportData> {
  const cid = target.masterCustomerId;
  if (cid == null) throw new Error("계정 정보를 불러올 수 없어요");

  // ↓↓ 기존 buildReportBytes의 수집 부분을 그대로 옮긴다 (brandP ~ shProducts).
  //    병렬 구조(promise 먼저 출발 + 소비자에서 각자 await)는 성능 감사 2026-07-02 확정 —
  //    순서·동시성을 바꾸지 말 것.

  // ... (기존 코드 그대로) ...

  return {
    model, searchTypes, displayData, campGroups,
    plKeywords, shKeywords, shProducts,
    shProductAdRows: productAdRows,
    shProductInfo: productInfo,
  };
}
```

**주의:** 기존 코드에서 `resolveProductInfo(...)`의 결과가 인라인으로 `buildProductRows`에 들어가고 있다. `shProductInfo`로 반환하려면 변수로 뽑아야 한다:

```ts
// 변경 전
const shProducts = buildProductRows(
  productAdRows,
  await resolveProductInfo(cid, productAdRows.map((a) => a.label)),
);

// 변경 후 — 동작 동일, info를 재사용할 수 있게 변수로만 뽑음
const productInfo = await resolveProductInfo(cid, productAdRows.map((a) => a.label));
const shProducts = buildProductRows(productAdRows, productInfo);
```

- [ ] **Step 3: `buildReportBytes`를 `collectReportData` 호출로 교체**

```ts
export async function buildReportBytes(target: ReportTarget, range: DateRange, meta: ReportMeta): Promise<Uint8Array> {
  const url = chrome.runtime.getURL("src/assets/report-template.xlsx");
  // 양식 로드와 데이터 수집을 동시에 출발 — 기존과 동일하게 수집이 양식 fetch를 기다리지 않는다.
  const filesP = (async (): Promise<ZipFiles> =>
    openXlsx(new Uint8Array(await (await fetch(url)).arrayBuffer())))();
  const dataP = collectReportData(target, range, meta);
  const files = await filesP;
  const {
    model, searchTypes, displayData, campGroups, plKeywords, shKeywords, shProducts,
  } = await dataP;

  // ↓↓ 이하 기존 fill*/render*/removeSheets/buildXlsx 코드 그대로
  // ...
}
```

- [ ] **Step 4: 타입 체크**

Run: `npm run typecheck`
Expected: 에러 없음

- [ ] **Step 5: 빌드**

Run: `npm run build`
Expected: 성공

- [ ] **Step 6: 동작 불변 검증 (필수)**

chrome://extensions에서 확장 Reload → Step 1과 **동일한 광고주·동일한 기간**으로 리포트를 다시 뽑는다. 새 파일을 `after.xlsx`로 저장.

```bash
cd "C:\Users\PC\AppData\Local\Temp\claude\C--dev-dvads"
# xlsx는 zip이라 mtime이 섞인다. 압축을 풀어 XML만 비교한다.
mkdir -p b a && cd b && unzip -oq ../baseline.xlsx && cd ../a && unzip -oq ../after.xlsx && cd ..
diff -r b a
```

Expected: **차이 없음** (출력 없음).

> 차이가 나오면 추출 과정에서 순서나 동시성이 바뀐 것이다. 되돌리고 다시 하라.
> `docProps/core.xml`의 작성 시각만 다르면 무시해도 된다(양식에 없으면 안 나온다).

- [ ] **Step 7: 커밋**

```bash
git add src/lib/report-build.ts
git commit -m "$(cat <<'EOF'
refactor(F-Report): collectReportData() 추출 — 수집과 엑셀 생성 분리

buildReportBytes가 데이터 수집과 엑셀 생성을 한 함수에서 섞고 있었다.
F-Brief(문구)는 수집만 필요하고 엑셀은 필요 없어 공유 지점을 만든다.

수집 순서·동시성(promise 먼저 출발 + 소비자에서 각자 await)은 성능 감사
2026-07-02 확정 구조라 그대로 보존. 양식 로드도 수집과 동시 출발시켜 기존과 동일.

F-Brief가 전기 상품을 소재ID로 매칭할 수 있게 shProductAdRows/shProductInfo 노출
(이미 함수 안에서 계산되던 값).

검증: 동일 계정·기간의 .xlsx를 추출 전후로 뽑아 압축 해제 후 diff -r — 차이 없음.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 5: 계정별 목표 ROAS 저장

**Files:**
- Modify: `src/types/storage.ts:163-180` (`MultiAccountUserMeta`)
- Modify: `src/options/multi-account-ui.tsx`

**Interfaces:**
- Produces: `MultiAccountUserMeta.targetRoas?: number`

**왜 자동 추정하지 않는가:** 계정 평균을 목표로 삼으면 계정이 통째로 부진할 때 전부 "정상"으로 나와 무용지물이 된다. 설계 §5.

- [ ] **Step 1: 타입에 필드 추가**

`src/types/storage.ts`의 `MultiAccountUserMeta` 안, `changeWatch` 다음 줄에:

```ts
  /**
   * 목표 광고수익률(%). F-Brief 키워드 구간 분류 기준.
   * undefined = 미설정 — 구간 분류 후보를 만들지 않는다(자동 추정 안 함).
   */
  targetRoas?: number;
```

- [ ] **Step 2: 옵션 페이지에 입력란 추가**

`src/options/multi-account-ui.tsx`에서 기존 비즈머니 임계값 입력란을 찾아 **같은 패턴으로** 목표 ROAS 입력을 추가한다.

- label: `목표 수익률`
- suffix: `%`
- placeholder: `800` (DESIGN.md Decisions Log 2026-05-22 - `"예: 800"`이 아니라 값만)
- 빈 값 → `undefined`로 저장 (미설정)

> 기존 `bizMoneyThreshold` 입력의 코드를 그대로 읽고 복제한다. 새 컴포넌트를 만들지 않는다.

- [ ] **Step 3: 타입 체크 + 빌드**

Run: `npm run typecheck && npm run build`
Expected: 에러 없음

- [ ] **Step 4: 수동 검증**

- [ ] chrome://extensions에서 Reload → 옵션 페이지 열기
- [ ] 계정 하나에 목표 수익률 `800` 입력 → 저장 → 페이지 새로고침 → **값이 남아 있는지**
- [ ] 빈 값으로 지우고 저장 → 새로고침 → **비어 있는지** (0이 아니라 미설정)

- [ ] **Step 5: 커밋**

```bash
git add src/types/storage.ts src/options/multi-account-ui.tsx
git commit -m "$(cat <<'EOF'
feat(F-Brief): 계정별 목표 광고수익률 저장 (MultiAccountUserMeta.targetRoas)

F-Brief 키워드 구간 분류(초록/노랑/무색) 기준. 보고 로그에 300%/600%/800%가
등장 — 계정마다 다르므로 계정별 저장.

미설정 시 자동 추정하지 않는다 — 계정 평균을 목표로 삼으면 계정이 통째로
부진할 때 전부 "정상"으로 나와 무용지물.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 6: 패널 + 진입점 (AI 없이 동작)

**Files:**
- Create: `src/lib/brief-data.ts`
- Create: `src/content/brief.ts`
- Create: `src/content/brief-panel.ts`
- Modify: `src/content/report-datepicker.ts` (`showAuthor?: boolean`)
- Modify: `src/content/multi-account.ts:2971` (행 메뉴 "세팅안 생성" 앞)
- Modify: `src/styles/overlay.css`

**Interfaces:**
- Consumes: `collectReportData` / `ReportData` (Task 4), `extractCandidates` / `BriefCandidate` (Task 2), `copyTablePng` (Task 3), `MultiAccountUserMeta.targetRoas` (Task 5)
- Produces:
  - `async function collectBriefData(target: ReportTarget, range: DateRange): Promise<BriefData>` (`brief-data.ts`)
  - `function buildSummaryText(data: BriefData): string` (`brief-data.ts`)
  - `function openBriefFlow(anchor: HTMLElement, target: ReportTarget): void` (`brief.ts`)
  - `function renderBriefPanel(opts: BriefPanelOpts): void` (`brief-panel.ts`)

**이 Task가 끝나면 AI 없이도 쓸 수 있다.** 요약 문구(설계 §3 1겹 - AI 미경유)와 색칠 표가 나온다. Task 9~10이 AI 문장을 얹는다.

**진입 흐름은 `src/content/report.ts`를 미러한다.** `running`/`runToken`/`stale()` 패턴, 진행 오버레이, `closePopover()`, `friendlyApiError` 전부 동일. **그 파일을 읽고 복제하라.**

- [ ] **Step 1: `report-datepicker.ts`에 담당자 숨김 옵션 추가**

`OpenDatePickerOpts`에 추가:

```ts
  /** 담당자 입력란 표시 여부. 기본 true. F-Brief는 문구에 담당자명이 안 들어가 false. */
  showAuthor?: boolean;
```

`openReportDatePicker` 안에서 담당자 입력 행을 만드는 코드를 `if (opts.showAuthor !== false)`로 감싼다. `onConfirm`의 `author`는 숨겼을 때 빈 문자열로 넘긴다.

- [ ] **Step 2: `brief-data.ts` 작성**

```ts
/**
 * F-Brief 데이터 — F-Report의 collectReportData를 재사용하고 엑셀만 건너뛴다.
 *
 * 요약 문구(인사/3지표/전기 대비)는 **AI를 거치지 않는다** — 코드가 문자열로 조립한다.
 * 물어본 적이 없으므로 AI가 이 숫자를 틀릴 확률은 0이다(설계 §3 1겹).
 */

import { collectReportData, type ReportData, type ReportTarget } from "./report-build";
import { previousRange, rangeText, type DateRange } from "./report-period";
import { roasPct } from "./brief-rules";
import { type ReportMetrics } from "./report-data";

export interface BriefData extends ReportData {
  range: DateRange;
  advertiserName: string;
}

export async function collectBriefData(target: ReportTarget, range: DateRange): Promise<BriefData> {
  // 담당자/작성일은 엑셀 표지 전용이라 문구엔 안 쓰인다. 빈 값으로 넘긴다.
  const data = await collectReportData(target, range, { authorName: "", createdDate: "" });
  return { ...data, range, advertiserName: target.name };
}

/** 기간 일수. "지난 30일 동안" 같은 표현에 쓴다. */
function dayCount(range: DateRange): number {
  const a = new Date(range.since).getTime();
  const b = new Date(range.until).getTime();
  return Math.round((b - a) / 86_400_000) + 1;
}

function won(n: number): string {
  return `${Math.round(n).toLocaleString()}원`;
}

/** 억/만 단위 반올림 — "약 34만 원 감소" 같은 표현용. 보고 로그의 관행. */
function approxWon(n: number): string {
  const abs = Math.abs(n);
  if (abs >= 100_000_000) return `약 ${(abs / 100_000_000).toFixed(1)}억 원`;
  if (abs >= 10_000) return `약 ${Math.round(abs / 10_000).toLocaleString()}만 원`;
  return `약 ${Math.round(abs).toLocaleString()}원`;
}

/**
 * 요약 블록 — 인사 + 기간/범위 + 3지표 + 전기 대비.
 * 보고 로그 5건이 전부 이 형태다. AI 미경유(설계 §3 1겹).
 */
export function buildSummaryText(data: BriefData): string {
  const cur = data.model.totalCurrent;
  const prev = data.model.totalPrev;
  const scope = data.model.hasDisplay ? "검색광고, GFA 포함" : "검색광고";
  const curRoas = roasPct(cur);
  const prevRoas = roasPct(prev);

  const lines = [
    "안녕하세요:)",
    "",
    `지난 ${dayCount(data.range)}일 동안 ${scope}`,
    "",
    `▶광고비 : ${won(cur.cost)}`,
    `▶전환매출액 : ${won(cur.revenue)}`,
    `▶광고수익률 : ${curRoas.toFixed(2)}%로 집계되었습니다.`,
  ];

  // 전기 데이터가 전무하면 비교 문장을 만들지 않는다(신규 계정 등).
  if (prev.cost > 0) {
    const diff = cur.revenue - prev.revenue;
    const dir = diff >= 0 ? "증가" : "감소";
    const roasDir = curRoas >= prevRoas ? "상승" : "하락";
    lines.push(
      "",
      `지난 동기간 대비 매출은 ${approxWon(diff)} ${dir}하였으며, 수익률 또한 ` +
        `${prevRoas.toFixed(0)}% > ${curRoas.toFixed(0)}%로 ${roasDir}하는 추세를 보였습니다.`,
    );
  }

  return lines.join("\n");
}

/** 요약 표 — 문구 ①에 딸리는 사진. */
export function buildSummarySpec(data: BriefData) {
  const rows = ([
    ["설정 기간", data.model.totalCurrent],
    ["이전 기간", data.model.totalPrev],
  ] as Array<[string, ReportMetrics]>).map(([label, m]) => ({
    cells: [
      label,
      m.impressions.toLocaleString(),
      m.clicks.toLocaleString(),
      won(m.cost),
      String(m.purchaseConv),
      won(m.revenue),
      `${roasPct(m).toFixed(0)}%`,
    ],
  }));
  return {
    title: `${data.advertiserName} · ${rangeText(data.range)}`,
    columns: ["구분", "노출", "클릭", "총비용", "구매완료", "매출액", "수익률"],
    rows,
  };
}

export { won, approxWon };
```

> `won`/`approxWon`을 export하는 이유: Task 10의 `brief.ts`가 `totals`를 만들 때 같은 형식을 써야 한다. 두 곳에서 따로 포맷하면 검산이 어긋난다 - AI에게 "267,558원"을 보내놓고 검산 집합엔 "267558.0"이 들어가는 식.

- [ ] **Step 3: `brief-panel.ts` 작성 — 블록 나열 + 블록별 복사**

카톡은 텍스트와 이미지를 한 메시지에 못 붙인다. **블록마다 복사 버튼**을 둔다. AE가 위에서부터 복사-붙여넣기를 반복하면 기존 보고 로그와 같은 모양이 나온다.

```ts
/**
 * F-Brief 패널 DOM — 블록 나열 + 블록별 복사.
 *
 * 카톡은 텍스트/이미지를 한 메시지에 못 붙인다. 그래서 "문구 하나 + 표 하나"가 아니라
 * 블록의 나열이고, 각 블록이 자기 복사 버튼을 갖는다(보고 로그의 문단-사진 1:1 구조).
 */

import { type BriefTableSpec } from "@/lib/brief-rules";
import { copyTablePng } from "@/lib/brief-table";
import { showToast } from "./toast";
import { wireBackdropDismiss } from "./dialog-dismiss";

export interface BriefTextBlock {
  type: "text";
  text: string;
  /** AI가 창작한 액션 문장이면 true — 좌측 주황 선. Task 10에서 채운다. */
  isAiJudgment?: boolean;
}
export interface BriefTableBlock {
  type: "table";
  spec: BriefTableSpec;
}
export type BriefBlock = BriefTextBlock | BriefTableBlock;

export interface BriefPanelOpts {
  advertiserName: string;
  blocks: BriefBlock[];
  /** "직접 고르기" 클릭. Task 10에서 후보 선택 화면으로. */
  onPickManually?: () => void;
}

let disposePanel: (() => void) | null = null;

export function closeBriefPanel(): void {
  disposePanel?.();
  disposePanel = null;
}

export function renderBriefPanel(opts: BriefPanelOpts): void {
  closeBriefPanel();

  const backdrop = document.createElement("div");
  backdrop.className = "dvads dvads-brief-backdrop";

  const card = document.createElement("div");
  card.className = "dvads-brief-card";

  const head = document.createElement("div");
  head.className = "dvads-brief-head";
  head.textContent = `보고 문구 - ${opts.advertiserName}`;
  card.appendChild(head);

  const body = document.createElement("div");
  body.className = "dvads-brief-body";

  for (const block of opts.blocks) {
    const wrap = document.createElement("div");
    wrap.className = "dvads-brief-block";

    if (block.type === "text") {
      const ta = document.createElement("textarea");
      ta.className = "dvads-brief-text";
      if (block.isAiJudgment) ta.classList.add("dvads-brief-block-ai");
      ta.value = block.text;
      // 내용에 맞춰 높이 — 스크롤바가 블록 안에 또 생기면 읽기 나쁘다.
      const fit = () => {
        ta.style.height = "auto";
        ta.style.height = `${ta.scrollHeight}px`;
      };
      ta.addEventListener("input", fit);
      wrap.appendChild(ta);

      const btn = document.createElement("button");
      btn.type = "button";
      btn.className = "dvads-btn dvads-brief-copy";
      btn.textContent = "복사";
      btn.addEventListener("click", () => {
        // 편집된 현재 값을 복사한다. 주황 선은 CSS라 텍스트에 안 딸려간다.
        void navigator.clipboard.writeText(ta.value)
          .then(() => showToast({ message: "문구를 복사했어요", variant: "success" }))
          .catch(() => showToast({ message: "복사하지 못했어요. 직접 선택해 복사해 주세요", variant: "error" }));
      });
      wrap.appendChild(btn);
      body.appendChild(wrap);
      // attach 후에 높이를 재야 scrollHeight가 나온다(detached면 0).
      requestAnimationFrame(fit);
    } else {
      const img = document.createElement("img");
      img.className = "dvads-brief-img";
      img.alt = block.spec.title;
      wrap.appendChild(img);

      const btn = document.createElement("button");
      btn.type = "button";
      btn.className = "dvads-btn dvads-brief-copy";
      btn.textContent = "이미지 복사";
      btn.addEventListener("click", () => {
        void copyTablePng(block.spec)
          .then(() => showToast({ message: "표 이미지를 복사했어요. 카카오톡에 붙여넣으세요", variant: "success" }))
          .catch((e) => {
            console.warn("[dv-ads/brief] 표 이미지 복사 실패", e);
            showToast({ message: "표 이미지를 복사하지 못했어요", variant: "error" });
          });
      });
      wrap.appendChild(btn);
      body.appendChild(wrap);

      // 미리보기 — 렌더 실패해도 복사 버튼은 살려둔다.
      void import("@/lib/brief-table").then(({ renderTablePng }) =>
        renderTablePng(block.spec).then((blob) => {
          img.src = URL.createObjectURL(blob);
        }),
      ).catch((e) => console.warn("[dv-ads/brief] 표 미리보기 실패", e));
    }
  }

  card.appendChild(body);

  const foot = document.createElement("div");
  foot.className = "dvads-brief-foot";
  if (opts.onPickManually) {
    const pick = document.createElement("button");
    pick.type = "button";
    pick.className = "dvads-btn";
    pick.textContent = "직접 고르기";
    pick.addEventListener("click", () => opts.onPickManually?.());
    foot.appendChild(pick);
  }
  const close = document.createElement("button");
  close.type = "button";
  close.className = "dvads-btn dvads-btn-primary";
  close.textContent = "닫기";
  close.addEventListener("click", () => closeBriefPanel());
  foot.appendChild(close);
  card.appendChild(foot);

  backdrop.appendChild(card);
  document.body.appendChild(backdrop);

  // 카드 안에서 텍스트 드래그 → backdrop에서 mouseup 시 잘못 닫히는 것 방지.
  // 직접 구현 금지 — 이 헬퍼가 mousedown 시작 위치를 추적한다.
  const unwire = wireBackdropDismiss(backdrop, () => closeBriefPanel());

  disposePanel = () => {
    unwire();
    backdrop.remove();
  };
}
```

> `wireBackdropDismiss`의 실제 시그니처를 `src/content/dialog-dismiss.ts`에서 확인하고 맞춰라. 반환값이 정리 함수가 아니면 그에 맞게 고친다.

- [ ] **Step 4: `brief.ts` 작성 — 진입 오케스트레이터**

`src/content/report.ts`의 `openReportFlow` / `runSingle`을 읽고 그 구조를 복제한다. `running`/`runToken`/`stale()`/진행 오버레이/`closePopover()`/`friendlyApiError` 동일.

```ts
/**
 * F-Brief — 광고주 보고 문구 생성 오케스트레이터 (콘텐츠 스크립트).
 *
 * F-MultiAccount popover 행 메뉴 "보고 문구"에서 진입. 계정 1개만(일괄은 범위 밖).
 * 기간 선택 → collectBriefData → 규칙 엔진 → 패널.
 *
 * 진행/취소 패턴은 report.ts와 동일 — 취소해도 진행 중 fetch는 못 멈추므로 토큰으로 무효화.
 */

import { showToast } from "./toast";
import { friendlyApiError } from "@/lib/friendly-error";
import { type ReportTarget } from "@/lib/report-build";
import { type DateRange } from "@/lib/report-period";
import { openReportDatePicker } from "./report-datepicker";
import { closePopover } from "./multi-account";
import { collectBriefData, buildSummaryText, buildSummarySpec } from "@/lib/brief-data";
import { extractCandidates } from "@/lib/brief-rules";
import { renderBriefPanel, type BriefBlock } from "./brief-panel";
import { loadUserMeta } from "@/lib/multi-account-storage";

let running = false;
let runToken = 0;

export function openBriefFlow(anchor: HTMLElement, target: ReportTarget): void {
  if (target.masterCustomerId == null) {
    showToast({ message: "이 계정 정보를 불러올 수 없어요. 페이지를 새로고침한 뒤 다시 시도해 주세요", variant: "error" });
    return;
  }
  if (running) return;
  openReportDatePicker({
    anchor,
    subText: target.name,
    showAuthor: false, // 문구에 담당자명이 안 들어간다
    onConfirm: (range) => void run(target, range),
  });
}

async function run(target: ReportTarget, range: DateRange): Promise<void> {
  if (running) return;
  running = true;
  const token = ++runToken;
  const stale = () => token !== runToken;
  closePopover();
  showProgress("보고 문구를 만드는 중...", cancelRun);
  try {
    const meta = await loadUserMeta(target.adAccountNo);
    const data = await collectBriefData(target, range);
    if (stale()) return;

    const candidates = extractCandidates({
      keywords: [...data.plKeywords, ...data.shKeywords],
      placements: data.model.byPlacement,
      targetRoas: meta?.targetRoas,
    });

    // Task 10 전까지는 요약 + 후보별 표만. AI 문장은 아직 없다.
    const blocks: BriefBlock[] = [
      { type: "text", text: buildSummaryText(data) },
      { type: "table", spec: buildSummarySpec(data) },
    ];
    for (const c of candidates) {
      blocks.push({ type: "table", spec: c.table });
    }

    hideProgress();
    renderBriefPanel({ advertiserName: target.name, blocks });

    if (meta?.targetRoas == null) {
      showToast({
        message: "목표 수익률을 설정하면 키워드 분류를 제안해요. 설정에서 계정별로 입력할 수 있어요",
        variant: "info",
      });
    }
  } catch (e) {
    console.warn("[dv-ads/brief] 보고 문구 생성 실패", e);
    if (stale()) return;
    hideProgress();
    showToast({ message: friendlyApiError(String(e), "test"), variant: "error" });
  } finally {
    if (!stale()) running = false;
  }
}
```

> `showProgress`/`hideProgress`/`cancelRun`은 `report.ts`에 있지만 export되지 않았다. **`report.ts`에서 export하고 `brief.ts`가 import한다.** 복제하지 마라 - 진행 오버레이가 두 개 생긴다.
> `loadUserMeta`의 실제 이름/시그니처를 `src/lib/multi-account-storage.ts`에서 확인하고 맞춰라.

- [ ] **Step 5: 행 메뉴에 "보고 문구" 추가**

`src/content/multi-account.ts`에서 `label: "세팅안 생성"` 블록을 찾아 그 **직전**에 추가. `keepOpen` + `anchorProxy` 패턴은 "리포트 생성"과 동일하게 복제한다 (동적 import 중 anchor가 떨어져 rect가 0이 되는 문제).

```ts
        {
          label: "보고 문구",
          keepOpen: true,
          onClick: (anchor) => {
            // "리포트 생성"과 동일 — keepOpen 메뉴라 onClick 직후 populate()가 anchor를 떼어낸다.
            // 동적 import resolve 시점엔 rect가 0이 되므로 지금(동기) 캡처해 프록시로 넘긴다.
            const rect = anchor.getBoundingClientRect();
            const anchorProxy = {
              getBoundingClientRect: () => rect,
              isConnected: false,
              contains: () => false,
            } as unknown as HTMLElement;
            void import("./brief")
              .then(({ openBriefFlow }) => {
                openBriefFlow(anchorProxy, {
                  adAccountNo: entry.adAccountNo,
                  masterCustomerId: entry.masterCustomerId,
                  name: meta?.displayName?.trim() || entry.name,
                });
              })
              .catch((e) => {
                console.warn("[dv-ads/brief] 보고 문구 화면을 열지 못함", e);
                showToast({ message: "보고 문구 화면을 열지 못했어요. 페이지를 새로고침한 뒤 다시 시도해 주세요", variant: "error" });
              });
          },
        },
```

- [ ] **Step 6: `overlay.css`에 패널 스타일 추가**

DESIGN.md 토큰 사용. 토스식 - 색이 아니라 여백과 선이 위계를 만든다.

```css
/* ── F-Brief 보고 문구 패널 ── */
.dvads-brief-backdrop {
  position: fixed; inset: 0; z-index: 2147483646;
  background: rgba(0, 0, 0, 0.32);
  display: flex; align-items: center; justify-content: center;
}
.dvads-brief-card {
  width: min(560px, calc(100vw - 32px));
  max-height: calc(100vh - 64px);
  display: flex; flex-direction: column;
  background: #fff; border-radius: 16px;
  box-shadow: 0 12px 40px rgba(0, 0, 0, 0.20), 0 2px 8px rgba(0, 0, 0, 0.10);
}
.dvads-brief-head {
  padding: 20px 24px 12px; font-size: 16px; font-weight: 600; color: #171717;
}
.dvads-brief-body {
  flex: 1; overflow-y: auto; padding: 0 24px;
  display: flex; flex-direction: column; gap: 20px;
}
.dvads-brief-block { position: relative; }
.dvads-brief-text {
  width: 100%; border: 0; resize: none; overflow: hidden;
  font: 400 14px/1.7 Pretendard, -apple-system, sans-serif;
  color: #171717; background: transparent; padding: 0;
}
.dvads-brief-text:focus { outline: none; }
/* AI 판단 문단 — 좌측 3px 주황 선. DESIGN.md Threshold Alert Cue(2026-05-22)와 동일 구조.
   배경을 칠하지 않는 이유: DV 주황 사용 면적 ~3% 규칙. 세로 선은 면적이 사실상 0. */
.dvads-brief-block-ai {
  padding-left: 12px;
}
.dvads-brief-block-ai::before {
  content: ""; position: absolute; left: 0; top: 0; bottom: 0;
  width: 3px; background: #E6783B; border-radius: 2px;
}
.dvads-brief-img {
  max-width: 100%; display: block;
  border: 1px solid #eef0f3; border-radius: 8px;
}
.dvads-brief-copy {
  margin-top: 8px;
}
.dvads-brief-foot {
  display: flex; justify-content: flex-end; gap: 8px;
  padding: 16px 24px 20px;
}
```

> `.dvads-brief-block-ai`는 `<textarea>`에 붙는데 `::before`는 textarea에 안 먹는다. **`wrap`(`.dvads-brief-block`)에 붙여야 한다** - Step 3의 `ta.classList.add`를 `wrap.classList.add`로 고쳐라.

- [ ] **Step 7: 타입 체크 + 빌드**

Run: `npm run typecheck && npm run build`
Expected: 에러 없음

- [ ] **Step 8: 수동 검증 (필수)**

chrome://extensions Reload → `ads.naver.com/manage/ad-accounts/` → 계정 popover.

- [ ] 행 메뉴에 "보고 문구"가 "세팅안 생성" 위에 보임
- [ ] 클릭 → 기간 선택기가 열리고 **담당자 입력란이 없음**
- [ ] 기간 확인 → 진행 오버레이 → 패널이 뜸
- [ ] 요약 문구의 광고비/매출/수익률이 **광고관리자 실제 값과 일치**
- [ ] 디스플레이 없는 계정 → "검색광고, GFA 포함"이 아니라 **"검색광고"**
- [ ] 목표 수익률 미설정 계정 → 안내 토스트가 뜨고 분류 표가 없음
- [ ] 목표 수익률 800 설정한 계정 → 미달 키워드 표가 나오고 **초록/노랑 색칠이 보임**
- [ ] "복사" → 카카오톡에 붙여넣기 → **주황 선이 안 딸려감**
- [ ] "이미지 복사" → 카카오톡에 붙여넣기 → **표가 선명함**(흐리면 dpr 확인)
- [ ] 패널 안에서 텍스트 드래그 → 바깥에서 마우스 놓기 → **패널이 안 닫힘**
- [ ] 진행 중 취소 → 즉시 다시 실행 가능

- [ ] **Step 9: 커밋**

```bash
git add src/lib/brief-data.ts src/content/brief.ts src/content/brief-panel.ts src/content/report-datepicker.ts src/content/report.ts src/content/multi-account.ts src/styles/overlay.css
git commit -m "$(cat <<'EOF'
feat(F-Brief): 패널 + 진입점 — AI 없이 요약 문구 + 색칠 표

행 메뉴 "보고 문구"(리포트/세팅안과 나란히). 계정 1개만.
collectReportData 재사용, 엑셀 생성만 건너뜀.

요약 문구(인사/3지표/전기대비)는 AI 미경유 — 코드가 문자열 조립.
물어본 적이 없으므로 숫자가 틀릴 확률 0(설계 §3 1겹).

블록마다 복사 버튼 — 카톡은 텍스트/이미지를 한 메시지에 못 붙인다.
보고 로그의 문단-사진 1:1 구조를 그대로 재현.

기간 선택기에 showAuthor 옵션 추가(문구엔 담당자명 미사용).
진행 오버레이는 report.ts에서 export해 공유 — 복제하면 2개가 뜬다.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 7: 순위 보강 (`highRoasLowRank`)

**Files:**
- Modify: `src/lib/brief-rules.ts` (+ `.test.ts`)
- Modify: `src/content/brief.ts`

**Interfaces:**
- Consumes: `estimateRank` from `@/lib/rank`, `MAX_POSITION_BY_DEVICE` from `@/types/storage`
- Produces:
  - `const LOW_RANK_FLOOR = 6`
  - `function pickRankTargets(rows: BriefKeywordRow[], targetRoas?: number): BriefKeywordRow[]`
  - `extractCandidates`가 `highRoasLowRank` 후보를 추가로 낸다

**순위 조회는 후보로 좁힌 뒤에만.** 전체 키워드에 걸면 수백 회 호출이다. 비용 임계를 넘고 green 구간인 키워드로 먼저 좁힌다(실측 수십 개). F-Setup이 같은 패턴을 쓴다 - `src/content/setup.ts:321-360`을 읽고 복제하라.

**제약:** 자격증명 미등록 시 이 후보만 조용히 스킵. 다른 후보는 정상 동작. `MAX_POSITION_BY_DEVICE` 준수(PC 1~10, MOBILE 1~5) - 초과가 배치에 하나라도 섞이면 전체 400.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/brief-rules.test.ts`에 추가:

```ts
import { pickRankTargets, LOW_RANK_FLOOR } from "./brief-rules";

describe("pickRankTargets", () => {
  it("LOW_RANK_FLOOR는 6", () => {
    expect(LOW_RANK_FLOOR).toBe(6);
  });

  it("비용 임계 이상 + green 구간만 순위 조회 대상 — 수백 회 호출 방지", () => {
    const targets = pickRankTargets(flattenKeywords(GROUPS), 800);
    expect(targets.map((t) => t.keyword)).toEqual(["대나무자리"]); // ROAS 900% = green
  });

  it("목표 미설정이면 대상 없음 — green 판정이 불가능하다", () => {
    expect(pickRankTargets(flattenKeywords(GROUPS), undefined)).toEqual([]);
  });
});

describe("extractCandidates - highRoasLowRank", () => {
  const withRank = (rank?: number): KeywordGroup[] => [{
    campaign: "C", group: "G",
    keywords: [{ keyword: "고효율", metrics: m(50_000, 5, 450_000) }],
  }];

  it("green + 저순위면 후보", () => {
    const rows = flattenKeywords(withRank());
    rows[0].rank = 8;
    const c = extractCandidates({ keywords: [], placements: [], targetRoas: 800, rankedRows: rows })
      .find((x) => x.kind === "highRoasLowRank");
    expect(c!.facts.keywords).toBe("고효율");
  });

  it("green이어도 순위가 높으면 후보 아님", () => {
    const rows = flattenKeywords(withRank());
    rows[0].rank = 2;
    expect(extractCandidates({ keywords: [], placements: [], targetRoas: 800, rankedRows: rows })
      .find((x) => x.kind === "highRoasLowRank")).toBeUndefined();
  });

  it("순위를 못 얻었으면 후보 아님 — 자격증명 미등록 시 조용히 스킵", () => {
    const rows = flattenKeywords(withRank());
    expect(extractCandidates({ keywords: [], placements: [], targetRoas: 800, rankedRows: rows })
      .find((x) => x.kind === "highRoasLowRank")).toBeUndefined();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL

- [ ] **Step 3: 구현**

`brief-rules.ts`의 `BriefRuleInput`에 추가:

```ts
  /** 순위가 보강된 키워드 행. brief.ts가 pickRankTargets 대상만 채워 넘긴다. */
  rankedRows?: BriefKeywordRow[];
```

파일에 추가:

```ts
/** 이 순위 이하(숫자가 큼)면 "낮다"고 본다. 로그의 "2페이지"는 PC 11위~지만 보수적으로 6위. */
export const LOW_RANK_FLOOR = 6;

/**
 * 순위를 조회할 키워드만 고른다 — **전체에 걸면 수백 회 호출이다.**
 * 비용 임계를 넘고 목표를 달성한(green) 키워드만. 실측 수십 개 수준.
 */
export function pickRankTargets(rows: BriefKeywordRow[], targetRoas?: number): BriefKeywordRow[] {
  if (targetRoas == null || targetRoas <= 0) return [];
  return rows.filter((r) =>
    r.metrics.cost >= COST_FLOOR && roasBand(roasPct(r.metrics), targetRoas) === "green",
  );
}
```

`extractCandidates` 안, `zeroConvPlacement` 블록 **앞**에 추가:

```ts
  // ④ 목표를 달성했는데 순위가 낮음 → 상향 여지. **F001 순위 + F-Report 효율 결합.**
  // 순위를 못 얻었으면(자격증명 미등록 등) rank가 undefined라 자연히 후보가 안 만들어진다.
  if (targetRoas != null && targetRoas > 0 && input.rankedRows) {
    const lowRank = input.rankedRows
      .filter((r) => r.rank != null && r.rank >= LOW_RANK_FLOOR &&
        roasBand(roasPct(r.metrics), targetRoas) === "green")
      .sort(byCostDesc);
    if (lowRank.length > 0) {
      out.push({
        kind: "highRoasLowRank",
        facts: {
          기준: `목표 수익률 ${targetRoas}% 달성, 추정 순위 ${LOW_RANK_FLOOR}위 이하`,
          keywords: lowRank.map((r) => r.keyword).join(", "),
          count: lowRank.length,
          평균순위: Math.round(lowRank.reduce((s, r) => s + (r.rank ?? 0), 0) / lowRank.length),
        },
        table: {
          title: "목표 달성 · 순위 상승 여지 키워드",
          columns: [...KW_COLUMNS, "추정순위"],
          rows: lowRank.map((r) => {
            const base = kwRow(r, targetRoas);
            return { ...base, cells: [...base.cells, `${r.rank}위`] };
          }),
        },
        selected: false,
      });
    }
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: `brief.ts`에 `fillRanks` 작성**

**`src/content/setup.ts:321-360`을 먼저 읽어라.** 거기 `GET_BID_ESTIMATE`를 보내고 `estimateRank`로 순위를 매기는 코드가 이미 있다. 배치 크기·에러 처리·device 처리를 그대로 복제한다. 새로 설계하지 마라.

`brief.ts`에 추가:

```ts
import { estimateRank } from "@/lib/rank";
import { MAX_POSITION_BY_DEVICE, type RankPosition } from "@/types/storage";
import { type BriefKeywordRow } from "@/lib/brief-rules";

/**
 * 순위 보강 — 대상 행의 rank를 제자리에서 채운다(반환 없음).
 *
 * setup.ts:321-360의 GET_BID_ESTIMATE 패턴을 복제한다. 배치에 cap 초과 position이
 * 하나라도 섞이면 전체 400이므로 MAX_POSITION_BY_DEVICE를 반드시 지킨다(PC 1~10).
 * 순위를 못 얻은 행은 rank를 undefined로 남긴다 — extractCandidates가 자연히 건너뛴다.
 */
async function fillRanks(rows: BriefKeywordRow[]): Promise<void> {
  const DEVICE = "PC" as const; // 보고 로그의 "2페이지"는 PC 기준
  const maxPos = MAX_POSITION_BY_DEVICE[DEVICE];

  // setup.ts와 동일하게 (키워드, 실효입찰가) 중복 제거 후 배치 호출.
  // ⚠️ 실효 입찰가가 필요하다 — 우리 rows에는 지표만 있고 입찰가가 없다.
  //    ncc/keywords에서 bidAmt를 가져와야 한다(useGroupBidAmt면 그룹 bidAmt 상속).
  //    setup-data.ts의 collectKeywordBidPairs가 같은 일을 한다 — 읽고 맞춰라.
  // ... setup.ts:321-360 패턴 복제 ...
  // 응답의 1~maxPos위 시장가 → estimateRank(userBid, rankToBid) → row.rank에 대입.
  // estimateRank가 "out"을 반환하면 rank를 채우지 않는다(순위권 밖 = 상향 여지 판단 불가).
}
```

> **주의:** `pickRankTargets`가 고른 행에는 **입찰가가 없다.** `BriefKeywordRow`는 advanced-report에서 온 지표만 담는다. `estimateRank(userBid, rankToBid)`에 넘길 `userBid`를 `ncc/keywords`에서 따로 가져와야 한다. `src/lib/setup-data.ts`의 `collectKeywordBidPairs`가 정확히 이 문제를 이미 푼다 - 그 코드를 읽고 재사용하라. 이게 이 Task에서 가장 품이 드는 부분이다.

- [ ] **Step 6: `brief.ts`에 순위 보강 연결**

`run()` 안, `extractCandidates` 호출 **앞**에:

```ts
    // 순위 보강 — 후보로 좁힌 뒤에만 조회한다(전체면 수백 회 호출).
    // 실패해도 다른 후보는 살린다 — 자격증명 미등록이 흔하다.
    const allRows = flattenKeywords([...data.plKeywords, ...data.shKeywords]);
    const rankTargets = pickRankTargets(allRows, meta?.targetRoas);
    if (rankTargets.length > 0) {
      try {
        await fillRanks(rankTargets);
      } catch (e) {
        console.warn("[dv-ads/brief] 순위 보강 실패 — 순위 후보만 생략", e);
      }
    }
    if (stale()) return;
```

`extractCandidates` 호출에 `rankedRows: allRows` 추가.

- [ ] **Step 7: 타입 체크 + 빌드**

Run: `npm run typecheck && npm run build`
Expected: 에러 없음

- [ ] **Step 8: 수동 검증 (필수)**

- [ ] 자격증명 **등록** 상태 + 목표 800 → "순위 상승 여지 키워드" 표에 추정순위 열이 보임
- [ ] 옵션에서 자격증명 **삭제** → 다시 실행 → **다른 표는 그대로 나오고 순위 표만 없음** (에러 토스트 없음)
- [ ] SW Console에 `[searchad]` 400 에러 없음 (있으면 `MAX_POSITION_BY_DEVICE` 위반)
- [ ] 키워드 200개 이상인 계정에서 실행 → 순위 호출이 수십 회 수준 (Network 탭)
- [ ] 표의 추정순위가 F001 팝오버가 같은 키워드에 표시하는 순위와 **일치**

- [ ] **Step 9: 커밋**

```bash
git add src/lib/brief-rules.ts src/lib/brief-rules.test.ts src/content/brief.ts
git commit -m "$(cat <<'EOF'
feat(F-Brief): highRoasLowRank — 목표 달성인데 순위 낮은 키워드

F001 순위 추정 + F-Report 효율의 결합. 두 데이터가 한 확장 안에 있어서
가능한 것 — 광고관리자도 이 조합을 제공하지 않는다.
임유엽 AE가 엑셀 색칠하며 손으로 찾던 초록색 후보가 이것.

순위 조회는 후보로 좁힌 뒤에만(pickRankTargets) — 전체면 수백 회 호출.
비용 임계 초과 + green 구간만. 실측 수십 개.

자격증명 미등록 시 rank가 undefined라 자연히 후보가 안 만들어진다.
다른 후보는 정상 동작 — 에러로 전체를 막지 않는다.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 8: `productConvDrop` — 전기 대비 전환 빠진 상품

**Files:**
- Modify: `src/lib/brief-data.ts`
- Modify: `src/lib/brief-rules.ts` (+ `.test.ts`)
- Modify: `src/content/brief.ts`

**Interfaces:**
- Consumes: `shProductAdRows` / `shProductInfo` (Task 4), `fetchAdvancedReport` / `CAMPAIGN_TP_CODE` from `@/lib/report-data`, `buildProductAdRows` from `@/lib/report-build`
- Produces:
  - `interface BriefProductDelta { label: string; cur: ReportMetrics; prev: ReportMetrics }`
  - `async function fetchPrevProducts(...): Promise<NamedMetrics[]>` (`brief-data.ts`)
  - `extractCandidates`가 `productConvDrop` 후보를 추가로 낸다

**보고 로그의 "객단가가 높은 [온열 찜질기] 상품에서 전환이 발생하지 않아"가 이 후보다.** 로그 전체에서 가장 값어치 있는 진단 문장이다.

**왜 추가 수집이 필요한가:** `collectReportData`는 설정 기간 상품만 모은다(F-Report가 그것만 쓴다). 전기 상품은 F-Brief만 필요하므로 `collectReportData`를 건드리지 않고 `brief-data.ts`가 별도로 1회 더 호출한다. `collectReportData`의 동작 불변을 지킨다.

**현재 기간에 있는 상품만 후보로 삼는다.** 이름은 `shProductInfo`(현재 기준)에서만 얻을 수 있다. 현재에 아예 없는 상품(노출 0)은 이름을 못 얻어 문구에 쓸 수 없다 - 범위 밖.

- [ ] **Step 1: 실패하는 테스트 작성**

`src/lib/brief-rules.test.ts`에 추가:

```ts
import { type BriefProductDelta } from "./brief-rules";

describe("extractCandidates - productConvDrop", () => {
  const products: BriefProductDelta[] = [
    // 전환 5 → 0, 매출 -340,000 → 후보
    { label: "온열 찜질기", cur: m(80_000, 0, 0), prev: m(75_000, 5, 340_000) },
    // 전환 유지 → 후보 아님
    { label: "대나무 돗자리", cur: m(50_000, 4, 300_000), prev: m(50_000, 4, 310_000) },
    // 전환 증가 → 후보 아님
    { label: "17MM", cur: m(60_000, 8, 500_000), prev: m(60_000, 3, 200_000) },
    // 전환 감소하나 매출 낙폭이 임계 미만 → 후보 아님 (소음)
    { label: "소품", cur: m(20_000, 1, 30_000), prev: m(20_000, 2, 35_000) },
  ];
  const base = { keywords: [] as KeywordGroup[], placements: [] as NamedMetrics[], targetRoas: 800 };

  it("전환이 줄고 매출 낙폭이 임계 이상인 상품만", () => {
    const c = extractCandidates({ ...base, products }).find((x) => x.kind === "productConvDrop");
    expect(c!.facts.products).toBe("온열 찜질기");
  });

  it("매출 낙폭이 임계 미만이면 제외 — 소음 방지", () => {
    const c = extractCandidates({ ...base, products }).find((x) => x.kind === "productConvDrop");
    expect(String(c!.facts.products)).not.toContain("소품");
  });

  it("표에 전기/현재를 나란히 — 비교가 문장의 근거다", () => {
    const c = extractCandidates({ ...base, products }).find((x) => x.kind === "productConvDrop");
    expect(c!.table.columns).toContain("이전 구매완료");
  });

  it("상품 데이터가 없으면 후보 없음", () => {
    expect(extractCandidates(base).find((x) => x.kind === "productConvDrop")).toBeUndefined();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL

- [ ] **Step 3: `brief-rules.ts` 구현**

```ts
/** 매출 낙폭이 이 값 미만이면 후보로 안 만든다 — 소음 방지. */
export const REVENUE_DROP_FLOOR = 100_000;

export interface BriefProductDelta {
  label: string;
  cur: ReportMetrics;
  prev: ReportMetrics;
}
```

`BriefRuleInput`에 추가:

```ts
  /** 상품별 현재/전기 지표. 현재 기간에 존재하는 상품만(이름을 얻을 수 있는 것만). */
  products?: BriefProductDelta[];
```

`extractCandidates`의 `return out;` 앞에 추가:

```ts
  // ⑤ 전기 대비 전환이 빠진 상품 — 보고 로그의 "객단가 높은 [온열 찜질기]에서 전환이
  // 발생하지 않아"가 이것. 매출 낙폭 임계로 소음을 거른다.
  if (input.products) {
    const dropped = input.products
      .filter((p) =>
        p.cur.purchaseConv < p.prev.purchaseConv &&
        p.prev.revenue - p.cur.revenue >= REVENUE_DROP_FLOOR,
      )
      .sort((a, b) => (b.prev.revenue - b.cur.revenue) - (a.prev.revenue - a.cur.revenue));
    if (dropped.length > 0) {
      out.push({
        kind: "productConvDrop",
        facts: {
          기준: "이전 기간 대비 구매완료 전환 감소",
          products: dropped.map((p) => p.label).join(", "),
          count: dropped.length,
          매출감소합계: dropped.reduce((s, p) => s + (p.prev.revenue - p.cur.revenue), 0),
        },
        table: {
          title: "상품별 성과 (이전 기간 대비)",
          columns: ["상품", "총비용", "구매완료", "이전 구매완료", "매출액", "이전 매출액", "수익률"],
          rows: dropped.map((p) => ({
            cells: [
              p.label,
              `${p.cur.cost.toLocaleString()}원`,
              String(p.cur.purchaseConv),
              String(p.prev.purchaseConv),
              `${p.cur.revenue.toLocaleString()}원`,
              `${p.prev.revenue.toLocaleString()}원`,
              `${roasPct(p.cur).toFixed(0)}%`,
            ],
            band: targetRoas != null ? roasBand(roasPct(p.cur), targetRoas) : undefined,
          })),
        },
        selected: false,
      });
    }
  }
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: `brief-data.ts`에 전기 상품 수집 추가**

`report-build.ts`의 `productReportP` 부분(`fetchAdvancedReport` + `nccAdId` attributes + `CAMPAIGN_TP_CODE.쇼핑검색` 필터)을 읽고 **전기 range로 동일하게** 호출하는 함수를 만든다. 응답은 `buildProductAdRows`(이미 export되어 있음)로 `NamedMetrics[]`(소재ID 기준)로 바꾼다.

import에 추가:

```ts
import { collectReportData, buildProductAdRows, type ReportData, type ReportTarget } from "./report-build";
import { fetchAdvancedReport, CAMPAIGN_TP_CODE, ZERO_METRICS, type ReportMetrics } from "./report-data";
import { type NamedMetrics } from "./report-fill";
import { roasPct, type BriefProductDelta } from "./brief-rules";
```

그리고 `collectBriefData`를 고친다:

```ts
export interface BriefData extends ReportData {
  range: DateRange;
  advertiserName: string;
  /** 현재 기간에 존재하는 상품의 현재/전기 지표. 이름은 현재 기준으로만 얻을 수 있다. */
  products: BriefProductDelta[];
}

export async function collectBriefData(target: ReportTarget, range: DateRange): Promise<BriefData> {
  const cid = target.masterCustomerId;
  if (cid == null) throw new Error("계정 정보를 불러올 수 없어요");
  // 전기 상품은 F-Brief만 필요하다 — collectReportData를 건드리지 않고 여기서 1회 더 부른다.
  // 두 수집을 동시에 출발시켜 왕복을 더하지 않는다.
  const [data, prevAdRows] = await Promise.all([
    collectReportData(target, range, { authorName: "", createdDate: "" }),
    fetchPrevProducts(cid, previousRange(range)).catch((e) => {
      console.warn("[dv-ads/brief] 전기 상품 조회 실패 — 상품 후보만 생략", e);
      return [] as NamedMetrics[];
    }),
  ]);

  // 소재ID로 매칭. shProducts(ProductRow)는 이름 조인 후라 ID가 없어 못 쓴다.
  const prevById = new Map(prevAdRows.map((r) => [r.label, r.metrics]));
  const products: BriefProductDelta[] = data.shProductAdRows.map((cur) => {
    const info = data.shProductInfo.get(cur.label);
    return {
      label: info?.productTitle ?? cur.label,
      cur: cur.metrics,
      prev: prevById.get(cur.label) ?? ZERO_METRICS,
    };
  }).filter((p) => p.label !== "" );

  return { ...data, range, advertiserName: target.name, products };
}
```

> `ProductInfo`의 실제 필드명(`productTitle` 등)을 `report-build.ts:592`에서 확인하고 맞춰라.

- [ ] **Step 6: `brief.ts`에 연결**

`extractCandidates` 호출에 `products: data.products` 추가.

- [ ] **Step 7: 타입 체크 + 빌드**

Run: `npm run typecheck && npm run build`
Expected: 에러 없음

- [ ] **Step 8: 수동 검증 (필수)**

- [ ] 쇼핑검색 캠페인이 있는 계정으로 실행 → "상품별 성과 (이전 기간 대비)" 표가 나옴
- [ ] 표의 상품명이 **소재ID가 아니라 실제 상품명**
- [ ] 쇼핑검색이 없는 계정 → 상품 표가 없고 **에러 없음**
- [ ] Network 탭에서 `advanced-report` 호출이 전기 상품 1회만 늘어남

- [ ] **Step 9: 커밋**

```bash
git add src/lib/brief-rules.ts src/lib/brief-rules.test.ts src/lib/brief-data.ts src/content/brief.ts
git commit -m "$(cat <<'EOF'
feat(F-Brief): productConvDrop — 전기 대비 전환 빠진 상품

보고 로그의 "객단가 높은 [온열 찜질기]에서 전환이 발생하지 않아" — 로그
전체에서 가장 값어치 있는 진단 문장.

전기 상품은 F-Brief만 필요하므로 collectReportData를 건드리지 않고
brief-data가 1회 더 호출(동작 불변 유지). 두 수집은 동시 출발.

소재ID로 매칭 — shProducts(ProductRow)는 이름 조인 후라 ID가 없다.
현재 기간에 있는 상품만(이름을 얻을 수 있는 것만).
매출 낙폭 10만원 임계로 소음 제거.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 9: Supabase Edge Function

**Files:**
- Create: `supabase/functions/brief-compose/index.ts`
- Modify: `manifest.config.ts` (`host_permissions`)

**Interfaces:**
- Produces: `POST /functions/v1/brief-compose` → `{ blocks: [{ text, isAiJudgment }] }`

**Edge Function 1개만.** DB 테이블 없음, Auth 없음, Storage 없음. `@supabase/supabase-js`를 다시 넣지 않는다 - 단순 HTTPS POST라 `fetch`로 충분하다. 2026-05-15에 제거한 의존성을 번들만 키우며 되살릴 이유가 없다. 설계 §6.2.

**말투 샘플이 핵심이다.** "간결하고 정중하게 써라"라고 *설명*하는 것보다 실제 문구를 *보여주는* 쪽이 압도적으로 잘 맞는다. `안녕하세요:)`의 이모티콘까지 복제된다.

- [ ] **Step 1: Supabase 프로젝트 생성**

본 확장 전용 프로젝트를 만든다 (2026-05-15 "인프라 분리 결정" - 자매 프로젝트와 공유하지 않는다).

프로젝트 ref와 anon key를 기록. 환경변수 설정:

```bash
supabase secrets set ANTHROPIC_API_KEY=<키>
supabase secrets set BRIEF_TOKENS=<AE1토큰>,<AE2토큰>,<AE3토큰>
```

- [ ] **Step 2: Edge Function 작성**

`supabase/functions/brief-compose/index.ts`:

```ts
// F-Brief AI 조립 — 사내 AE 전용. 사실 목록을 디브이 말투 문장으로 옮기기만 한다.
//
// AI는 "분석가"가 아니라 "번역기"다. 분석은 확장의 규칙 엔진이, 판단은 AE가 한다.
// 여기 오는 facts는 AE가 체크한 것만이다 — 안 보낸 건 지어낼 재료가 없다(설계 §3 2겹).
//
// 저장하지 않는다. 로그에 남기지 않는다 (광고주 데이터).

const TOKENS = new Set((Deno.env.get("BRIEF_TOKENS") ?? "").split(",").map((s) => s.trim()).filter(Boolean));
const API_KEY = Deno.env.get("ANTHROPIC_API_KEY") ?? "";

// 실제 AE 보고 로그. 톤을 설명하지 말고 보여준다.
const TONE_SAMPLES = `
안녕하세요:)

지난 30일 동안 검색광고, GFA 포함

▶광고비 : 267,558원
▶전환매출액 : 1,660,420원
▶광고수익률 : 620.58%로 집계되었습니다.

지난 동기간 대비 매출은 약 34만 원 감소하였으며, 수익률 또한 862% > 621%로 하락하는 추세를 보였습니다.

다만, 이는 객단가가 높은 [온열 찜질기] 상품에서 전환이 발생하지 않아 전체 매출 및 수익률이 하락한 영향으로 판단됩니다.

이에, 우선 현재 입찰가를 유지하며 추이를 확인한 뒤, 지속적으로 성과가 저조할 경우 입찰가 조정 진행하겠습니다.
---
최근 30일간 광고비 1만 원 이상 소진되었으나 전환이 발생하지 않은 '대나무돗자리' 키워드는 저효율 그룹으로 이동하여 운영하고자 합니다.

다만, 전환이 발생한 주요 키워드들의 노출 순위가 2페이지로 다소 낮게 확인되어, 해당 그룹의 입찰가는 소폭 상향 조정하여 운영하겠습니다.
---
해당 캠페인의 경우 현재 목표 수익률 300% 미만으로 다소 저조한 것을 확인했습니다.

특히, 캠페인 총비용 절반 이상을 소진한 [#돋보기] 단독 키워드 그룹에서 수익률이 300% 미만으로 저조합니다.

이에, 추가적인 입찰가 하향 조정을 통해 수익률을 개선해 볼 수 있도록 하겠습니다.
`.trim();

const ACTION_LABEL: Record<string, string> = {
  raise: "입찰가 상향 조정",
  hold: "입찰가 유지 후 추이 확인",
  lower: "입찰가 하향 조정",
  exclude: "제외 처리",
  ask: "광고주 확인 요청",
};

Deno.serve(async (req) => {
  if (req.method !== "POST") return new Response("Method Not Allowed", { status: 405 });

  const auth = req.headers.get("authorization") ?? "";
  const token = auth.replace(/^Bearer\s+/i, "").trim();
  if (!token || !TOKENS.has(token)) {
    return new Response(JSON.stringify({ error: "unauthorized" }), {
      status: 401, headers: { "content-type": "application/json" },
    });
  }

  const body = await req.json();
  const facts = (body.facts ?? []) as Array<Record<string, unknown>>;
  if (facts.length === 0 && !body.memo) {
    return new Response(JSON.stringify({ blocks: [] }), { headers: { "content-type": "application/json" } });
  }

  const factLines = facts.map((f) => {
    const action = typeof f.action === "string" ? (f.actionText || ACTION_LABEL[f.action] || f.action) : "";
    const detail = Object.entries(f.facts ?? {}).map(([k, v]) => `${k}: ${v}`).join(" / ");
    return `- ${detail}${action ? ` → 액션: ${action}` : ""}`;
  });
  if (body.memo) factLines.push(`- AE 메모: ${body.memo}`);

  const prompt = [
    "너는 디브이마케팅 AE다. 아래 말투로 광고주 보고 문구를 써라.",
    "",
    "[말투 샘플]",
    TONE_SAMPLES,
    "",
    "[이번 데이터]",
    `광고주: ${body.advertiser}`,
    `기간: ${body.periodText}`,
    `광고비 ${body.totals?.cost} / 전환매출 ${body.totals?.revenue} / ROAS ${body.totals?.roas}%`,
    `이전 기간 ROAS ${body.prevTotals?.roas}%`,
    "",
    "[말할 것]",
    ...factLines,
    "",
    "[규칙]",
    "- 위에 준 사실 외에는 쓰지 마라. 시즌·트렌드 등 일반 상식을 끼워 넣지 마라.",
    "- 숫자를 바꾸지 마라. 위에 없는 숫자를 만들지 마라.",
    "- 인사말과 지표 요약은 쓰지 마라 — 이미 따로 만들었다. 진단과 액션만 써라.",
    "- 말할 것 하나당 문단 하나. 문단은 빈 줄로 구분.",
    "- em dash(—)를 쓰지 마라. 일반 하이픈(-)만.",
    "",
    "JSON만 출력: {\"blocks\":[{\"text\":\"문단\",\"isAiJudgment\":true}]}",
    "isAiJudgment는 그 문단에 액션 선언·판단이 들어갔으면 true, 데이터 서술뿐이면 false.",
  ].join("\n");

  const res = await fetch("https://api.anthropic.com/v1/messages", {
    method: "POST",
    headers: {
      "content-type": "application/json",
      "x-api-key": API_KEY,
      "anthropic-version": "2023-06-01",
    },
    body: JSON.stringify({
      model: "claude-sonnet-5",
      max_tokens: 2000,
      messages: [{ role: "user", content: prompt }],
    }),
  });

  if (!res.ok) {
    console.error("anthropic error", res.status); // 상태코드만 — 본문엔 광고주 데이터가 있다
    return new Response(JSON.stringify({ error: "upstream" }), {
      status: 502, headers: { "content-type": "application/json" },
    });
  }

  const data = await res.json();
  const text = data.content?.[0]?.text ?? "";
  const json = text.match(/\{[\s\S]*\}/)?.[0] ?? '{"blocks":[]}';
  return new Response(json, { headers: { "content-type": "application/json" } });
});
```

- [ ] **Step 3: 배포**

```bash
supabase functions deploy brief-compose --no-verify-jwt
```

`--no-verify-jwt`인 이유: Supabase Auth를 안 쓴다. 인증은 함수 안의 토큰 화이트리스트로 한다.

- [ ] **Step 4: 동작 확인**

```bash
curl -i -X POST "https://<ref>.supabase.co/functions/v1/brief-compose" \
  -H "Authorization: Bearer <AE1토큰>" -H "content-type: application/json" \
  -d '{"advertiser":"테스트","periodText":"2026.06.01 ~ 2026.06.30","totals":{"cost":"267,558원","revenue":"1,660,420원","roas":"620.58"},"prevTotals":{"roas":"862.53"},"facts":[{"facts":{"기준":"광고비 10,000원 이상 소진, 구매완료 전환 0건","keywords":"대나무돗자리","count":1},"action":"exclude"}]}'
```

Expected: 200 + `{"blocks":[{"text":"...대나무돗자리...","isAiJudgment":true}]}`

- [ ] **Step 5: 잘못된 토큰 거부 확인**

```bash
curl -i -X POST "https://<ref>.supabase.co/functions/v1/brief-compose" \
  -H "Authorization: Bearer wrong" -H "content-type: application/json" -d '{"facts":[]}'
```

Expected: `401`

- [ ] **Step 6: `manifest.config.ts`에 host_permissions 추가**

`host_permissions` 배열에 추가:

```ts
  "https://<ref>.supabase.co/*",
```

- [ ] **Step 7: 빌드**

Run: `npm run build`
Expected: 성공. `dist/manifest.json`에 도메인이 들어갔는지 확인.

- [ ] **Step 8: 커밋**

```bash
git add supabase/functions/brief-compose/index.ts manifest.config.ts
git commit -m "$(cat <<'EOF'
feat(F-Brief): Supabase Edge Function brief-compose

Edge Function 1개만. DB/Auth/Storage 미사용. @supabase/supabase-js 미복원 —
단순 HTTPS POST라 fetch로 충분(2026-05-15 제거 의존성을 번들만 키우며 되살릴 이유 없음).

인증은 사내 AE 토큰 화이트리스트(환경변수). --no-verify-jwt로 배포.
AI 키·프롬프트·말투 샘플 전부 서버에 — 확장 재배포 없이 튜닝 가능.

말투 샘플에 실제 AE 보고 로그를 그대로 박음. 톤을 설명하는 것보다
보여주는 쪽이 압도적으로 잘 맞는다.
프롬프트에 "일반 상식을 끼워 넣지 마라" 명시(설계 §3의 남는 구멍).

저장·로깅 없음(광고주 데이터). 에러 로그도 상태코드만.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 10: AI 조립 + 검산 + 후보 선택 화면

**Files:**
- Create: `src/lib/brief-verify.ts` (+ `.test.ts`)
- Create: `src/lib/brief-compose.ts`
- Modify: `src/content/brief.ts`
- Modify: `src/content/brief-panel.ts`
- Modify: `src/options/index.tsx` (AE 토큰 입력)

**Interfaces:**
- Consumes: `BriefCandidate` (Task 2), Edge Function (Task 9)
- Produces:
  - `function extractNumbers(text: string): string[]` (`brief-verify.ts`)
  - `function verifyBlock(text: string, allowed: Set<string>): boolean` (`brief-verify.ts`)
  - `async function composeBlocks(req: ComposeRequest): Promise<BriefTextBlock[]>` (`brief-compose.ts`)

**검산은 차단하지 않고 표시만 한다.** 오탐이 있다 - "30일", "2페이지" 같은 정상 숫자가 문장에 들어간다. 판단은 AE에게 맡긴다. 설계 §6.5.

- [ ] **Step 1: 검산 테스트 작성**

`src/lib/brief-verify.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { extractNumbers, verifyBlock } from "./brief-verify";

describe("extractNumbers", () => {
  it("쉼표 있는 숫자를 정규화해서 뽑는다", () => {
    expect(extractNumbers("광고비 267,558원")).toContain("267558");
  });

  it("소수점을 보존한다", () => {
    expect(extractNumbers("수익률 620.58%")).toContain("620.58");
  });

  it("여러 개를 다 뽑는다", () => {
    expect(extractNumbers("862% > 621%")).toEqual(["862", "621"]);
  });

  it("숫자가 없으면 빈 배열", () => {
    expect(extractNumbers("안녕하세요")).toEqual([]);
  });
});

describe("verifyBlock", () => {
  const allowed = new Set(["267558", "620.58", "1"]);

  it("허용 집합의 숫자만 있으면 통과", () => {
    expect(verifyBlock("광고비 267,558원, 수익률 620.58%", allowed)).toBe(true);
  });

  it("허용 집합에 없는 숫자가 있으면 실패", () => {
    // AI가 267,559로 바꿔 쓴 경우
    expect(verifyBlock("광고비 267,559원", allowed)).toBe(false);
  });

  it("숫자가 없으면 통과", () => {
    expect(verifyBlock("추이를 확인하겠습니다", allowed)).toBe(true);
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `npm test`
Expected: FAIL

- [ ] **Step 3: `brief-verify.ts` 구현**

```ts
/**
 * F-Brief 검산 — AI가 쓴 문장의 숫자가 우리가 준 값인지 대조 (설계 §3 3겹).
 *
 * **차단하지 않고 표시만 한다.** 오탐이 있다 — "30일", "2페이지" 같은 정상 숫자가
 * 문장에 들어간다. 판단은 AE에게 맡긴다.
 */

/** 문장에서 숫자를 뽑아 정규화(쉼표 제거, 소수점 보존). */
export function extractNumbers(text: string): string[] {
  const found = text.match(/\d[\d,]*(?:\.\d+)?/g) ?? [];
  return found.map((s) => s.replace(/,/g, ""));
}

/** 문장의 숫자가 전부 허용 집합에 있으면 true. */
export function verifyBlock(text: string, allowed: Set<string>): boolean {
  return extractNumbers(text).every((n) => allowed.has(n));
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `npm test`
Expected: PASS

- [ ] **Step 5: `brief-compose.ts` 작성**

Edge Function을 호출하고 검산을 적용한다. `facts`의 모든 값 + `totals`를 허용 집합으로 만든다.

**URL은 Task 9에서 만든 실제 프로젝트 ref로 채운다.** `<ref>`를 그대로 두면 컴파일은 되고 런타임에 죽는다. `manifest.config.ts`의 `host_permissions`와 **같은 도메인**이어야 한다 - 다르면 요청이 차단된다.

```ts
import { extractNumbers, verifyBlock } from "./brief-verify";
import { type BriefCandidate } from "./brief-rules";

// ⚠️ Task 9에서 만든 실제 Supabase 프로젝트 ref로 교체할 것.
//    manifest.config.ts의 host_permissions와 동일 도메인이어야 한다.
const FN_URL = "https://<ref>.supabase.co/functions/v1/brief-compose";

export interface ComposeRequest {
  advertiser: string;
  periodText: string;
  totals: Record<string, string>;
  prevTotals: Record<string, string>;
  selected: BriefCandidate[];
  memo: string;
}

export interface ComposedBlock {
  text: string;
  isAiJudgment: boolean;
  /** 검산 실패 — 우리가 안 준 숫자가 문장에 있다. 차단하지 않고 AE에게 표시만. */
  numberWarning: boolean;
}

async function loadToken(): Promise<string> {
  const { brief_token } = await chrome.storage.local.get("brief_token");
  if (!brief_token) throw new Error("보고 문구 이용 코드가 등록되어 있지 않아요. 설정에서 등록해 주세요");
  return brief_token as string;
}

export async function composeBlocks(req: ComposeRequest): Promise<ComposedBlock[]> {
  const token = await loadToken();
  const res = await fetch(FN_URL, {
    method: "POST",
    headers: { "content-type": "application/json", authorization: `Bearer ${token}` },
    body: JSON.stringify({
      advertiser: req.advertiser,
      periodText: req.periodText,
      totals: req.totals,
      prevTotals: req.prevTotals,
      // 체크된 후보의 facts만. 리포트 전체를 보내지 않는다(설계 §3 2겹).
      facts: req.selected.map((c) => ({ facts: c.facts, action: c.action, actionText: c.actionText })),
      memo: req.memo,
    }),
  });
  if (res.status === 401) throw new Error("보고 문구 이용 코드가 올바르지 않아요. 설정에서 다시 확인해 주세요");
  if (!res.ok) throw new Error("문구를 만들지 못했어요. 잠시 후 다시 시도해 주세요");

  const data = await res.json();

  // 허용 숫자 집합 = 우리가 보낸 모든 값에서 뽑은 숫자.
  const allowed = new Set<string>();
  for (const v of [...Object.values(req.totals), ...Object.values(req.prevTotals)]) {
    extractNumbers(String(v)).forEach((n) => allowed.add(n));
  }
  for (const c of req.selected) {
    for (const v of Object.values(c.facts)) {
      extractNumbers(String(v)).forEach((n) => allowed.add(n));
    }
  }
  extractNumbers(req.memo).forEach((n) => allowed.add(n));

  return (data.blocks ?? []).map((b: { text: string; isAiJudgment?: boolean }) => ({
    text: b.text,
    isAiJudgment: b.isAiJudgment === true,
    numberWarning: !verifyBlock(b.text, allowed),
  }));
}
```

- [ ] **Step 6: 옵션 페이지에 AE 토큰 입력 추가**

`src/options/index.tsx`의 검색광고 자격증명 입력 카드 **아래**에 카드를 하나 더 추가한다. 기존 카드 코드를 읽고 복제하라.

- 제목: `보고 문구 이용 코드`
- 설명: `사내에서 발급받은 코드를 입력하면 보고 문구를 만들 수 있어요.`
- 필드 1개, `chrome.storage.local`의 `brief_token`에 저장.
- **"토큰"/"API 키" 같은 영문 기술용어를 쓰지 않는다.**

- [ ] **Step 7: 패널에 검산 경고 + 후보 선택 화면**

`brief-panel.ts`의 `BriefTextBlock`에 추가:

```ts
  /** 검산 실패 — 우리가 안 준 숫자가 있다. 차단하지 않고 배지만. */
  numberWarning?: boolean;
```

렌더에서 `numberWarning`이면 블록에 경고 배지를 붙인다:

```ts
      if (block.numberWarning) {
        const warn = document.createElement("div");
        warn.className = "dvads-brief-warn";
        warn.textContent = "확인 필요 - 데이터에 없는 숫자가 있어요";
        wrap.appendChild(warn);
      }
```

`overlay.css`에 추가:

```css
.dvads-brief-warn {
  margin-top: 6px; font-size: 12px; font-weight: 500; color: #DC2626;
}
```

**후보 선택 화면** — `renderBriefPanel`에 `mode: "auto" | "pick"`를 추가한다. `"pick"`이면 blocks 대신 후보 목록을 그린다:

- 후보마다 체크박스 + 라벨(`facts.기준`) + 액션 dropdown
- **dropdown은 `createDropdown`(`ui-dropdown.ts`) 의무.** native `<select>` 금지.
- 하단에 자유 입력(`memo`) textarea 1개 - "데이터가 모르는 맥락"
- `[AI에게 맡기기]` / `[문구 만들기]` 버튼

- [ ] **Step 8: `brief.ts`에 AI 조립 연결**

완전자동이 기본이다. `run()`에서 후보를 뽑은 뒤 **전부 selected=true로 놓고** `composeBlocks`를 부른다. AI가 액션까지 고르게 하려면 `action`을 비운 채 보낸다.

실패해도 요약 + 표는 살린다:

```ts
    let aiBlocks: ComposedBlock[] = [];
    try {
      aiBlocks = await composeBlocks({
        advertiser: target.name,
        periodText: rangeText(range),
        totals: { cost: won(cur.cost), revenue: won(cur.revenue), roas: curRoas.toFixed(2) },
        prevTotals: { roas: prevRoas.toFixed(2) },
        selected: candidates.map((c) => ({ ...c, selected: true })),
        memo: "",
      });
    } catch (e) {
      console.warn("[dv-ads/brief] AI 조립 실패 — 요약과 표만 표시", e);
      showToast({ message: String(e instanceof Error ? e.message : e), variant: "error" });
    }
```

`ComposedBlock`(`brief-compose.ts`)과 `BriefTextBlock`(`brief-panel.ts`)은 **다른 타입이다.** 변환해서 넣는다:

```ts
    // 후보별 AI 문단을 그 후보의 표 **앞**에 (보고 로그의 문단-사진 순서).
    // aiBlocks[i]와 candidates[i]는 Edge Function이 "말할 것 하나당 문단 하나"로
    // 만들므로 순서가 대응한다. 개수가 어긋나면(AI가 문단을 합치거나 쪼갬) 남는 표는
    // 뒤에 붙인다 — 표를 잃는 것보다 순서가 틀리는 게 낫다.
    const blocks: BriefBlock[] = [
      { type: "text", text: buildSummaryText(data) },
      { type: "table", spec: buildSummarySpec(data) },
    ];
    candidates.forEach((c, i) => {
      const ai = aiBlocks[i];
      if (ai) {
        blocks.push({
          type: "text",
          text: ai.text,
          isAiJudgment: ai.isAiJudgment,
          numberWarning: ai.numberWarning,
        });
      }
      blocks.push({ type: "table", spec: c.table });
    });
    // AI가 후보보다 문단을 많이 만든 경우 남은 문단을 뒤에 붙인다.
    for (const ai of aiBlocks.slice(candidates.length)) {
      blocks.push({ type: "text", text: ai.text, isAiJudgment: ai.isAiJudgment, numberWarning: ai.numberWarning });
    }
```

- [ ] **Step 9: 타입 체크 + 빌드**

Run: `npm run typecheck && npm test && npm run build`
Expected: 전부 통과

- [ ] **Step 10: 수동 검증 (필수)**

- [ ] 옵션에서 이용 코드 **미등록** → 실행 → 요약 + 표는 나오고 안내 토스트
- [ ] 이용 코드 **잘못 등록** → 실행 → "올바르지 않아요" 토스트, 요약/표는 나옴
- [ ] 이용 코드 **정상** → AI 문단이 표 앞에 들어감
- [ ] AI 문단 중 액션 선언 문단에 **좌측 주황 선**이 보임
- [ ] 그 문단 "복사" → 카톡 → **주황 선이 안 딸려감**
- [ ] 생성된 문구를 실제 보고 로그와 비교 → **말투가 비슷한지** (핵심 성공 기준)
- [ ] "직접 고르기" → 후보 목록 + 액션 dropdown → 몇 개만 체크 → "문구 만들기" → **체크한 것만 문장에 나옴**
- [ ] 자유 입력에 "6월 말부터 CPC 낮춰 운영 중" → **그 내용이 문장에 반영됨**
- [ ] 액션 dropdown이 **native select가 아니라 우리 dropdown**
- [ ] Network 탭 → Edge Function 요청 본문에 **체크 안 한 후보가 없는지**

- [ ] **Step 11: 커밋**

```bash
git add src/lib/brief-verify.ts src/lib/brief-verify.test.ts src/lib/brief-compose.ts src/content/brief.ts src/content/brief-panel.ts src/options/index.tsx src/styles/overlay.css
git commit -m "$(cat <<'EOF'
feat(F-Brief): AI 조립 + 숫자 검산 + 후보 선택 화면

완전자동이 기본(AI가 후보/액션까지) + "직접 고르기"로 AE선택 모드.
내부적으로 같은 엔진 — 완전자동은 체크박스를 AI가 미리 채운 상태일 뿐.

검산(3겹): AI 문장의 숫자를 우리가 보낸 값 집합과 대조. 차단하지 않고
배지만 — "30일"/"2페이지" 같은 정상 숫자 오탐이 있어 판단은 AE에게.

AI 판단 문단에 좌측 3px 주황 선(DESIGN.md Threshold Alert Cue 확장).
기술로 못 막는 구간(일반 상식 문장)의 마지막 방어선 — 최종 방아쇠는 사람이 당긴다.

AI 조립 실패해도 요약과 표는 살린다 — 가치의 8할이 규칙 엔진에 있다.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## Task 11: 문서 갱신

**Files:**
- Modify: `docs/PRD.md`
- Modify: `CLAUDE.md`
- Modify: `docs/DESIGN.md`
- Modify: `docs/ROADMAP.md`

**문서에 남은 원칙과 실제 동작이 어긋나면 나중에 사고가 난다.** 설계 §12.

- [ ] **Step 1: `docs/PRD.md` 개정**

- "사용자 데이터 외부 전송 0건" → **"광고 데이터는 네이버와 사내 서버(Supabase Edge Function) 외로 나가지 않는다. AI 문구 생성 시 AE가 체크한 사실만 전송한다."**
- "본 확장은 서버 DB를 보유하지 않는다" → 유지하되 **"Edge Function 1개를 경유하나 저장하지 않는다"**를 덧붙인다.
- "MVP 이후 기능(제외)" 목록 정리 - "데이터 내보내기(CSV/엑셀)"는 F-Report로 이미 실현됐다.

- [ ] **Step 2: `CLAUDE.md` Architecture에 F-Brief 추가**

`src/content/report.ts` 항목 근처에 한 항목 추가. 기존 항목들의 밀도·형식을 따른다:

```
- `src/content/brief.ts` + `brief-panel.ts` + `src/lib/brief-{rules,table,data,compose,verify}.ts` — F-Brief 광고주 보고 문구(AX 1호). F-MultiAccount 행 메뉴 "보고 문구" → 기간 선택 → `collectReportData` 재사용(엑셀 생성 제외) → 규칙 엔진이 후보 추출 → AI가 문장만 조립 → 블록별 복사(카톡은 텍스트/이미지를 한 메시지에 못 붙임). **AI는 분석가가 아니라 번역기** — ①인사/3지표/전기대비는 AI 미경유(코드가 문자열 조립), ②체크된 사실만 전송, ③숫자 검산. 못 막는 구간(일반 상식 문장)은 좌측 3px 주황 선으로 AE에게 표시. 분석 표는 캡처가 아니라 canvas 생성. 목표 ROAS는 `MultiAccountUserMeta.targetRoas`(미설정 시 구간 분류 후보 비활성, 자동 추정 안 함). 순위 보강은 후보로 좁힌 뒤에만(전체면 수백 회 호출). 서버는 Supabase Edge Function 1개 + AE 토큰 화이트리스트, `@supabase/supabase-js` 미사용.
```

Gotchas의 "사용자 데이터(광고 키워드·예산·소재 등) 외부 전송 0건이어야 한다"도 같은 취지로 조정.

- [ ] **Step 3: `docs/DESIGN.md` Decisions Log에 한 줄**

| 2026-07-16 | F-Brief AI 판단 문단에 **좌측 3px 주황 세로 선**(`.dvads-brief-block-ai`). 텍스트 색은 검은색 유지, 복사 시 미포함. 주황 배경 폐기. | 기술로 막을 수 없는 구간(AI가 일반 상식으로 끼워 넣는 문장)을 AE 눈에 띄게 하는 마지막 방어선. 문단 배경을 주황으로 칠하면 "DV 주황 ~3% 이내"를 크게 넘고, 2026-05-19 디바이스 토글 결정과 같은 brand color 인플레이션. 세로 선은 면적이 사실상 0. 2026-05-22 Threshold Alert Cue와 동일 구조라 새 패턴이 아님(색만 `#DC2626`→`#E6783B`). |

- [ ] **Step 4: `docs/ROADMAP.md`에 F-Report + F-Brief 추가**

**F-Report가 로드맵에 없다** (최종 갱신 2026-05-20, Task 019에서 멈춤). F-Brief를 추가하면서 F-Report도 함께 기록한다. Task 020(F-Report, 완료) / Task 021(F-Brief).

Task 013 보류 사유의 *"다음 brainstorming에서 새 기능 ID로 재정의 예정"*이 F-Brief로 실현됐음을 명시한다.

- [ ] **Step 5: 커밋**

```bash
git add docs/PRD.md CLAUDE.md docs/DESIGN.md docs/ROADMAP.md
git commit -m "$(cat <<'EOF'
docs(F-Brief): PRD 전송 원칙 개정 + CLAUDE/DESIGN/ROADMAP 갱신

PRD "외부 전송 0건" → "네이버와 사내 서버 외로 나가지 않는다. AE가 체크한
사실만 전송". 서버 중계를 택한 순간 피할 수 없는 변경 — 문서에 남은 원칙과
실제 동작이 어긋나면 나중에 사고가 난다.

ROADMAP에 F-Report가 아예 없어(2026-05-20 Task 019에서 멈춤) 함께 기록.
Task 013 보류 사유의 "새 기능 ID로 재정의 예정"이 F-Brief로 실현됨을 명시.

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 완료 기준

**성공 기준: AE가 생성된 문구를 거의 그대로 카톡에 붙여넣는다.**

실패 모드는 명확하다 - "그럴듯한데 우리 말투가 아니라서 결국 다 다시 씀". 그러면 시간을 아끼는 게 아니라 오히려 쓴다. **첫 사용자는 실제 보고 로그를 제공한 AE로 한다.**

말투가 안 맞으면 Edge Function의 `TONE_SAMPLES`를 그 AE의 실제 문구로 교체한다 - **확장 재배포 없이 고칠 수 있다.** 그러라고 서버에 뒀다.

## 설계와 다르게 구현했다면

`docs/superpowers/specs/2026-07-16-f-brief-design.md`를 고치고 커밋하라. 설계와 코드가 어긋난 채로 두지 마라.
